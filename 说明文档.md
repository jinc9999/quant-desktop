# 说明文档 — quant-desktop 币安合约量化交易桌面客户端

> 项目全生命周期管理唯一载体。每次打开项目前先阅读本文件，确认进度与要求后再开发。
> 最近更新时间：2026-08-08

---

## 开发规范

1. 每次打开项目前，必须先阅读本文档，确认当前进度与要求

## 一、项目规划

### 1.1 项目目标

基于 **Wails v3 + Go + Vue3 + TypeScript + vue-pure-admin** 构建币安合约量化交易桌面客户端。

- **Go 后端**：币安 WebSocket/REST 接口、策略运算、风控、SQLite 存储、委托生命周期管理
- **Vue3 前端**：策略配置、持仓展示、委托状态、交易日志、启停控制（纯 UI，不直接访问币安）
- **通信**：Wails 原生绑定（非 HTTP）
- **后台运行**：关闭前端窗口后 Go 策略持续运行
- **核心可靠性原则**：止损保护必须脱离 Bot 进程，由交易所真实委托单（STOP_MARKET + TRAILING_STOP_MARKET）独立承载

### 1.2 技术选型

| 决策点 | 选择 | 原因 |
| --- | --- | --- |
| 桌面框架 | Wails v3 (alpha) | 原生支持后台运行 |
| 前端基础 | pure-admin-thin | 已精简，保留布局+主题系统 |
| 币安 SDK | go-binance/v2 | Go 生态最成熟的币安库 |
| 数据库 | go-sqlite3 (CGO, WAL) | 用户指定 SQLite |
| 包管理 | pnpm | vue-pure-admin 官方推荐 |
| 前后端通信 | Wails Bindings + Events | 不新增 HTTP |

### 1.3 运行模式

- `DRY_RUN`：模拟下单，仍拉取真实行情；启动时自动运行策略
- `TESTNET`：币安测试网
- `LIVE`：实盘

---

## 二、实施方案

### 2.1 后端模块结构（internal/）

| 模块 | 文件 | 职责 |
| --- | --- | --- |
| binance | rest.go / ws.go / types.go | REST 下单查询、WS 行情、类型定义 |
| strategy | engine.go / screener.go / sliding_window.go | 策略主循环、筛选排名、增量缓存 |
| risk | manager.go / circuit_breaker.go | 风控状态机、多层熔断 |
| order | manager.go | 委托生命周期（创建/同步/取消/重试/崩溃恢复） |
| storage | db.go / *_repo.go | SQLite 初始化 + 各表 CRUD |
| bindings | service.go | Wails 绑定层，暴露给前端 |

### 2.2 委托止损核心数据流

```text
开仓: OpenLong → InsertPosition → orderMgr.PlaceStopOrders()
       ├─ 成功 → 挂 STOP_MARKET + TRAILING_STOP_MARKET，写入 orders + order_events
       └─ 失败(重试3次后) → CloseLong 回滚 + 取消已挂委托 + 告警
平仓: 币安触发委托 → SyncOrders 检测 FILLED → handleFilledOrder
       → ClosePosition + CancelRelatedOrders + EventTriggered
崩溃保护: 交易所委托独立生效；重启 RecoverOnStartup 对账
```

### 2.3 前端页面（frontend/src/views/）

- dashboard（仪表盘：账户状态/权益/运行时间/Tick统计/今日盈利）
- strategy（策略配置）、positions（实时持仓）、orders（委托状态）、logs（交易日志）

---

## 三、进度记录

### 3.1 里程碑状态

| 阶段 | 内容 | 状态 | 完成日期 |
| --- | --- | --- | --- |
| Phase 1 | 环境搭建 + 项目骨架 | ✅ 完成 | 2026-07 |
| Phase 2 | UI 框架搭建（深色主题 + 3 页面路由） | ✅ 完成 | 2026-07 |
| Phase 3 | Go 后端核心（存储/行情/策略/风控/持仓） | ✅ 完成 | 2026-07 |
| Phase 4 | Wails 绑定 + 前后端联调 | ✅ 完成 | 2026-07 |
| Phase 5 | 打包脚本（Windows/macOS） | ✅ 完成 | 2026-07 |
| 委托止损改造 | 交易所委托单独立承载止损保护 | ✅ 完成 | 2026-07-27 |
| 仪表盘数据展示 | Dashboard 页面 + 聚合 API + 错误计数 + 今日盈亏 | ✅ 完成 | 2026-07-28 |
| 自动化健康监控 | 30 分钟周期检查 + 引擎崩溃自动重启 + 错误率告警 | ✅ 完成 | 2026-07-28 |
| 持仓表格组件升级 | 11 列完整持仓表格（排序/筛选/展开/分页/合计）+ GetPositionDetails 聚合接口 | ✅ 完成 | 2026-07-28 |
| 已完成交易明细升级 | 10 列明细表格（含出场价/盈利%/手续费）+ exit_price/fee 入库 + 币安 userTrades 手续费接入 + GetClosedTradeDetails 接口 | ✅ 完成 | 2026-07-28 |
| 币安官方文档合规修复 | 双向持仓模式自动校验/设置 + 杠杆自动设置 + 跟踪止损改 quantity 方式 + 止损单 workingType=MARK_PRICE | ✅ 完成 | 2026-07-28 |
| Algo Order API 迁移 | 条件单从 /fapi/v1/order 迁移至 /fapi/v1/algoOrder | ✅ 完成 | 2026-07-30 |
| 策略升级（借鉴 ea-03） | 做空支持 + 3min 短窗口确认 + 1.5x 放量确认 + 跟踪止损调优 + 动态上移止损价 + 候选数量放开 | ✅ 完成 | 2026-07-30 |
| S01 纯追涨落地 | 锁定 S01（纯追涨·无门控）为唯一实盘策略，补固定止盈 10% + 最长持仓 120 分钟两项退出机制，默认参数全面对齐回测口径 | ✅ 完成 | 2026-08-04 |
| 新币过滤 | 新增新币过滤：exchangeInfo 上市日期缓存（onboardDate），筛选层+开仓层双保险过滤上市天数 ≤ 阈值（默认 60 天，可配置）的新合约，过滤日志写入 trade_logs（module=screener，每合约每进程一次） | ✅ 完成 | 2026-08-07 |
| 冷却期闭环修复 + 分原因冷却 | 条件单平仓（主平仓路径）从不通知引擎写冷却期 → 同币无限快速追单（实盘单币日开 40+ 次）；新增 OnClose(symbol,reason) 回调闭环 + CooldownAfterTrailingMin（止盈后 15 分钟可再入，止损后 30 分钟完整冷却） | ✅ 完成 | 2026-08-08 |
| S01 v2 全参数定稿 | 全参数矩阵（31 组单参 + 6 组扰动）：止损 4% / 止盈激活 2% / 回调 3% / 持仓 180 分 / 冷却 30 分 / 放量 1.2x；三年回测 +20,259U / PF 1.66 / 回撤 5.8%（1000 万口径，含追加仓）；固定止盈保持 0（tp>0 为灾难 PF 0.98） | ✅ 完成 | 2026-08-08 |
| 追加仓次数可配置 | 单币最大追加次数 MaxAddOnsPerSymbol 从硬编码 1 改为配置项（默认 2 = 同币最多 3 仓）；数据验证：2 次追加 +21,667U / 回撤 5.62%（vs 1 次 +20,259U / 5.80%），第 4 仓边际递减 | ✅ 完成 | 2026-08-08 |
| 止损单 -4130 修复 | 止损市价条件单 STOP_MARKET 从 ClosePosition(true) 改为按数量+ReduceOnly 挂单（与跟踪止损一致）：币安同一方向只允许一张 closePosition 条件单，同币多仓追加时第 2/3 仓必然 -4130 并触发回滚；修复后每仓独立数量止损互不冲突 | ✅ 完成 | 2026-08-08 |

### 3.2 验收结果（2026-07-27 实测）

| 验证项 | 命令 | 结果 |
| --- | --- | --- |
| Go 编译 | `go build ./...` | ✅ 退出码 0（仅模板遗留 ios/macOS 链接警告） |
| Go 全量测试 | `go test ./internal/...` | ✅ 全部通过（binance/bindings/order/risk/storage/strategy） |
| 前端生产构建 | `pnpm build` | ✅ 2008 模块，产物 2.25 MB，绑定 chunk 已打包 |
| 整体打包 | `wails3 build` | ✅ 生成 bin/quant-desktop（15 MB，Mach-O arm64） |
| 整体打包（S01） | `wails3 build` | ✅ 2026-08-04 生成 bin/quant-desktop（14 MB，Mach-O arm64），S01 纯追涨默认参数已内置，模拟盘可直接运行 |

### 3.3 变更记录

| 日期 | 变更内容 | 原因 |
| --- | --- | --- |
| 2026-07-27 | 新增 `TestFilledCloseLoop_DryRun`（internal/order/manager_test.go） | 原测试未覆盖「FILLED→平仓闭环」核心路径；DRY_RUN 下 GetOrderStatus 恒返回 NEW，需直接驱动 handleFilledOrder 验证：持仓 CLOSED(reason=STOP_LOSS)、关联委托取消、EventTriggered 写入。测试通过。 |
| 2026-07-27 | 创建本说明文档 | 按文档管理规范，建立项目全生命周期管理载体 |
| 2026-07-27 | **P0 并发开仓**（internal/strategy/engine.go）：openPositions 改为信号量限流（openConcurrency=6）的有界并发，抽取 openOne | 串行开满 30 仓需 ~90 次网络往返 ≈ 18s，超出 10s Tick 预算；并发后压缩回预算内 |
| 2026-07-27 | **修复 pos.ID bug**（engine.go openOne）：InsertPosition 后回填自增 ID | 原代码丢弃返回 ID，导致止损委托关联与回滚均指向 position_id=0 |
| 2026-07-27 | **P1 存储优化**：DSN 增加 `_synchronous=NORMAL`（storage/db.go）；runOnce 单次持仓查询复用给 open/monitor | WAL+NORMAL 为官方推荐组合，降低批量写入 fsync 开销；消除每 Tick 重复 GetOpenPositions |
| 2026-07-27 | **P2 WS 全量行情流**（internal/binance/ws.go）：新增 `!ticker@arr` 缓存 + GetTickers，runOnce 优先用 WS、空则回退 REST | 替代每 Tick 的 FetchTickers REST 轮询，消除常态主要耗时；回退保证可靠性 |
| 2026-07-27 | **分阶段埋点**：runOnce 输出 行情/筛选/开仓/监控 四段耗时 | 线上可观测各阶段开销，量化验证优化效果 |
| 2026-07-27 | 新增基准测试（internal/strategy/bench_test.go）：滑动窗口/筛选/并发开仓 | 提供可量化的性能验证手段 |
| 2026-07-28 | **仪表盘数据展示**：新增 Dashboard 页面（/dashboard，默认首页），6 张数据卡片展示账户状态、权益、今日盈利、运行时间、Tick 统计、扫描配置 | 用户需求：全面展示账户基础数据 |
| 2026-07-28 | **GetDashboardData 聚合接口**（bindings/service.go）：单次调用返回 running/mode/tickCount/tickErrorCount/startTime/runtimeSeconds/todayPnl/todayClosedCount/openPositionCount/余额数据 | 减少前端轮询次数，2s 刷新一次即可 |
| 2026-07-28 | **Tick 错误计数**（strategy/engine.go）：新增 tickErrorCount 字段，fetchTickers/GetOpenPositions 失败时递增 | 之前错误仅打日志无统计，仪表盘需展示 |
| 2026-07-28 | **今日盈亏聚合**（storage/position_repo.go）：GetTodayPnl 按本地时区零点聚合 SUM(realized_pnl)；GetClosedPositions 查询平仓历史 | 之前无按日期聚合的盈亏查询 |
| 2026-07-28 | **账户余额接口**（binance/rest.go）：GetFuturesBalance 调用 /fapi/v2/balance，DRY_RUN 返回模拟值 10000 USDT | 仪表盘展示账户权益需要 |
| 2026-07-28 | 新增测试：TestGetTodayPnl/TestGetClosedPositions（storage）、TestGetDashboardData/TestGetDashboardData_Running/TestGetClosedPositions_Empty（bindings）、代理配置测试 3 个 | 覆盖新增功能路径 |
| 2026-07-28 | **自动化健康监控**（bindings/health.go）：30 分钟周期检查引擎存活/Tick 错误率/WS 行情流状态；引擎崩溃自动重启；所有事件写入 trade_logs 表（module=health_monitor） | 用户需求：无人值守自动修复 |
| 2026-07-28 | 新增测试：TestHealthCheck_Normal/TestHealthCheck_EngineCrash/TestHealthCheck_NotStarted/TestLogHealthEvent（bindings） | 覆盖健康监控核心路径 |
| 2026-07-28 | **修复 #1 严重**：SyncOrders 从异步 goroutine 改为同步调用，在持仓查询前完成；monitorPositions 平仓前重新查询 DB 确认 OPEN 状态 | 消除重复平仓/反向开仓竞态 |
| 2026-07-28 | **修复 #2 严重**：handleFilledOrder 中 PnL 从硬编码 0 改为查询持仓 EntryPrice/Amount 计算 `(filledPrice - entryPrice) * amount` | 交易所触发的止损盈亏不再丢失 |
| 2026-07-28 | **修复 #3 高**：monitorPositions 中 CloseLong 返回值检查，失败时跳过 ClosePosition，下一 Tick 重试 | 防止交易所平仓失败但 DB 已标记平仓 |
| 2026-07-28 | **修复 #4 中**：ClosePosition SQL 添加 `AND status='OPEN'` 幂等守卫 | 竞态下 pnl=0 不再覆盖正确值 |
| 2026-07-28 | **修复 #5 低**：monitorPositions 激活条件从 `>` 改为 `>=`，与 risk/manager.go 一致 | 消除边界条件不一致 |
| 2026-07-28 | **修复 #6 低**：UpdateRiskState SQL 添加 `AND status='OPEN'` 守卫 | 已平仓持仓不再被更新风控状态 |
| 2026-07-28 | 新增 GetPositionByID（storage/position_repo.go）+ 3 个幂等性测试 | 支撑修复 #1/#2 的基础查询 |
| 2026-07-28 | **修复 monitorPositions 价格回退**：传入 priceMap 作为 WS GetPrice 的回退数据源 | WS 未连接时 GetPrice 返回 false，导致 17 个持仓止损从未被检查 |
| 2026-07-28 | **修复 DRY_RUN 余额**：GetDashboardData 从 DB 计算模拟余额（初始 10000 - 已用保证金 + 已实现盈亏 + 未实现盈亏），新增 GetTotalRealizedPnl | 之前硬编码 10000，不反映模拟交易状态 |
| 2026-07-28 | **交易流程重构**：参数更新 TrailingActivation 0.05→0.04, TrailingCallback 0.03→0.04 | 用户需求：激活价=买入价×1.04，回调 4% |
| 2026-07-28 | **移除 monitorPositions**（engine.go）：删除客户端本地止损检查，完全依赖交易所条件单 | 用户需求：取消机器人主动卖出，避免与交易所条件单重复 |
| 2026-07-28 | **SyncOrders 重构**（order/manager.go）：新增 priceMap 参数；按持仓分组；跟踪激活→撤固定止损；DRY_RUN 模拟成交（simulateDryRunFills）；LIVE 查交易所（syncExchangeOrders）；平仓闭环含 DB 记录 | 用户需求：双卖出单策略 + 条件单联动 + 每笔平仓保存数据库 |
| 2026-07-28 | **新增 Mode() getter**（binance/rest.go）：返回客户端运行模式 | SyncOrders 需区分 DRY_RUN/LIVE 逻辑 |
| 2026-07-28 | **新建"已完成交易"页面**（/trades）：展示平仓历史（交易对/方向/入场价/平仓原因/盈亏/时间），摘要卡片（总笔数/总盈亏/盈利笔/亏损笔） | 用户需求：已完成交易明细实时展示 |
| 2026-07-28 | **委托页面增强**（orders/index.vue）：新增激活价、回调率、成交价、持仓ID 列 | 用户需求：展示双卖出单完整信息 |
| 2026-07-28 | **持仓表格组件升级**（positions/index.vue 重写）：11 列完整展示（交易对/方向/入场价/入场时间/数量/保证金/标记价格/盈亏/回报率/止损价/委托状态）；表头点击排序（全量排序后分页）、交易对/方向/委托状态三重筛选、行展开（持仓详情+关联委托列表）、分页（10/20/50/100）、固定表头+双向滚动、合计行（总保证金/总盈亏）；盈亏绿涨红跌、价格自适应小数位、入场时间悬停显示完整时间戳；shallowRef+JSON 快照对比减少重渲染 | 用户需求：完整持仓表格组件（布局/展示/交互/视觉/性能五大类要求） |
| 2026-07-28 | **GetPositionDetails 聚合接口**（bindings/positions_view.go）：新增 PositionDetail/OrderBrief 视图模型，单次调用返回标记价格（WS 行情）、保证金（入场价×数量/杠杆）、未实现盈亏（多空方向区分）、回报率、聚合委托状态（deriveOrderStatus：部分成交>未成交/跟踪>已成交>已取消>无）及关联委托摘要 | 前端表格需一次性获取全部展示字段，减少轮询次数 |
| 2026-07-28 | **GetOrdersByPositionIDs 批量查询**（storage/order_repo.go）：单条 SQL 按 position_id IN (...) 批量查询，避免 N+1 | 支撑 GetPositionDetails 性能（100+ 持仓场景） |
| 2026-07-28 | 新增测试：TestGetOrdersByPositionIDs（storage）、TestGetPositionDetails/TestGetPositionDetails_Empty/TestDeriveOrderStatus 表驱动 7 分支（bindings）；bindings 重新生成（wails3 generate bindings -ts -i） | 覆盖新增接口与状态聚合规则全部分支 |
| 2026-07-28 | **已完成交易明细升级**（trades/index.vue 重写）：10 列展示（交易对/方向/入场价/出场价/数量/净盈亏/盈利%/手续费/平仓时间/平仓原因）；表头排序、交易对/方向/平仓原因三重筛选、分页（20/50/100）、固定表头+斑马纹；摘要卡片新增胜率与总手续费；盈亏与盈利%绿涨红跌、价格自适应小数位 | 用户需求：已完成交易明细需包含出场价/盈利%/手续费等 10 列 |
| 2026-07-28 | **positions 表新增 exit_price/fee 列**（storage/db.go）：建表 SQL 补充两列 + migratePositionsColumns 存量库幂等迁移（PRAGMA table_info 检测后 ALTER TABLE）；Position 结构体与 GetPositionByID/GetClosedPositions 扫描同步扩展 | 出场价与手续费需持久化，与 realized_pnl 口径一致 |
| 2026-07-28 | **ClosePosition 扩展签名**（storage/position_repo.go）：新增 exitPrice *float64 / fee float64 参数并写入 DB；同步更新 order/manager.go 平仓闭环与回滚调用、db_test.go 9 处测试调用 | 平仓时记录出场价与手续费 |
| 2026-07-28 | **币安手续费接入**（binance/rest.go）：新增 GetOrderFee，调用 /fapi/v1/userTrades（NewListAccountTradeService）累加 commission；DRY_RUN 返回 0，真实模式失败降级为 0 不阻断平仓 | 手续费数据来源（GetOrder 接口不返回手续费） |
| 2026-07-28 | **GetClosedTradeDetails 聚合接口**（bindings/trades_view.go）：新增 ClosedTradeDetail 视图模型，计算出票价/净盈亏/盈利%（净盈亏/保证金×100）/手续费；bindings 重新生成 | 前端表格一次性获取全部 10 列字段 |
| 2026-07-28 | **委托页新增状态筛选器**（views/orders/index.vue）：表格上方增加「全部/活跃/已成交/已取消」单选筛选（带实时计数），表格数据源切换为 filteredOrders，空状态文案随筛选态变化；默认全部，撤销后的 CANCELED 委托仍可查阅 | 用户需求：撤销后委托默认仍显示，需支持按需筛选活跃挂单 |
| 2026-07-28 | **币安官方文档合规审查与修复**：对照币安官方 USDⓈ-M 合约 API 文档（developers.binance.com）逐项核查挂单逻辑，发现并修复 4 处问题 | 用户需求：核对官方文档确认策略挂单是否有误 |
| 2026-07-28 | **修复①严重 双向持仓模式**（binance/rest.go + strategy/engine.go）：新增 GetPositionMode/SetPositionMode/EnsureHedgeMode；引擎 Start 时自动校验，单向持仓则切换为双向，失败写 error 日志告警 | 下单硬编码 positionSide=LONG 仅在双向持仓有效，币安默认单向持仓会报 -4061 拒单；原代码从不设置/校验 |
| 2026-07-28 | **修复②中 跟踪止损改 quantity**（binance/rest.go PlaceTrailingStop + order/manager.go）：签名新增 amount 参数，从 ClosePosition(true) 改为 Quantity(持仓数量)；PlaceStopOrders 传入 pos.Amount | 官方 closePosition 仅支持 STOP_MARKET/TAKE_PROFIT_MARKET，TRAILING_STOP_MARKET 应按数量平仓 |
| 2026-07-28 | **修复③中 止损触发价基准**（binance/rest.go）：PlaceStopMarket 与 PlaceTrailingStop 显式设置 WorkingType=MARK_PRICE | 原默认 CONTRACT_PRICE（最新价）易被瞬时插针误触发；标记价更稳健 |
| 2026-07-28 | **修复④中 杠杆自动设置**（binance/rest.go + strategy/engine.go）：新增 SetLeverage/EnsureLeverage（按交易对缓存仅首次设置）；openOne 开仓前调用，失败仅告警不阻断 | 原 config.Leverage 仅用于内部保证金计算，从不调交易所设置，实际杠杆可能与配置不符导致盈亏口径偏差 |
| 2026-07-28 | 新增测试：TestGetPositionMode_DryRun/TestEnsureHedgeMode_DryRun/TestSetLeverage_DryRun/TestEnsureLeverage_DryRun（binance）；更新 TestPlaceTrailingStop_DryRun 适配新签名 | 覆盖账户配置新接口 DRY_RUN 路径 |
| 2026-07-28 | **修复标记价格/盈亏/回报率无数据**（strategy/engine.go + binance/ws.go）：fetchTickers REST 回退时调用新增的 BackfillCache 方法将行情数据回填 WS 缓存，确保前端 GetPrice 能获取实时价格 | TESTNET 模式下 WS 连接失败，REST 回退未回填缓存导致前端 GetPrice 始终返回空 |
| 2026-07-28 | **修复 DRY_RUN OrderID 重复**（binance/rest.go）：新增 dryRunOrderID 原子计数器 + nextDryRunOrderID 方法（时间戳×1000+计数器），替代 time.Now().UnixMilli() | 同一毫秒内创建的两个委托单（止损+跟踪止损）OrderID 相同，导致数据库唯一约束冲突或状态混乱 |
| 2026-07-28 | **数据库模式隔离**（storage/db.go + bindings/service.go）：三种模式使用独立数据库文件（quant_dryrun.db / quant_testnet.db / quant_live.db）；新增 DBPathForMode 函数；SetCredentials 模式切换时自动关闭旧库、打开新库；GetSavedCredentials 支持跨模式查询凭据 | 用户需求：TESTNET/LIVE/DRY_RUN 数据完全隔离，避免模式切换后数据混乱 |
| 2026-07-30 | **全面系统检查与优化**：① rest.go 移除无用 spotClient 字段、清理 binance.UseDemo、添加合约 WS 代理设置（futures.SetWsProxyUrl）；② ws.go 移除死代码 Start 方法及多余导入；③ service.go 仪表盘数据新增 scanIntervalSec/timeframe 字段；④ dashboard/index.vue 从后端获取配置值替代硬编码；⑤ strategy/index.vue 修复 trailingCallback 默认值 3→4 与后端 0.04 一致 | 代码质量优化：清理死代码、统一前后端配置默认值、确保 WS 代理正确设置 |
| 2026-07-30 | **严重修复：恢复交易所侧止损单保护**：① openOne 开仓后立即调用 PlaceStopOrders 挂 STOP_MARKET + TRAILING_STOP_MARKET 委托（主保护）；② Start 启动时调用 EnsureOrdersForOpenPositions 为无保护持仓补挂止损；③ runOnce 每 Tick 调用 SyncOrders 同步交易所委托状态（检测止损触发）；④ closePosition 先撤交易所止损单再市价平仓（防止双向持仓模式下止损单反向开空）；⑤ monitorPositions 保留为备用保护 | GUA/USDT 亏损 224% 根因：系统未挂交易所止损单，仅靠 10s 轮询的 monitorPositions 判断止损，Bot 离线/行情中断时持仓零保护 |
| 2026-07-30 | **恢复 DRY_RUN 模式模拟逻辑**：rest.go 所有交易方法（OpenLong/CloseLong/PlaceStopMarket/PlaceTrailingStop/CancelOrder/GetOrderStatus/GetOrderFee/GetPositionMode/SetPositionMode/SetLeverage/GetOpenOrders/GetFuturesBalance）添加 isDryRun() 检查，返回模拟结果；新增 dryRunOrderID 原子计数器避免同毫秒 OrderID 重复 | 之前清理代码时误删 DRY_RUN 处理，导致测试直接调用真实 API 失败 |
| 2026-07-30 | **Algo Order API 迁移**：PlaceStopMarket/PlaceTrailingStop 从旧接口 /fapi/v1/order 迁移至 /fapi/v1/algoOrder（NewCreateAlgoOrderService）；新增 CancelAlgoOrder/GetAlgoOrderStatus/GetOpenAlgoOrders/UpdateStopMarketPrice；OrderResult/OrderInfo 新增 AlgoID/AlgoStatus/ActualOrderID；orders 表新增 algo_id 列（幂等迁移）；manager.go 取消/同步/对账按 AlgoID 分流 | 币安 2025-12-09 起条件单强制迁移至 Algo Order API，旧接口返回 -4120 |
| 2026-07-30 | **后台错误推送前端弹窗**：engine.go 新增 emitError 回调 → service.go emitBackendError → Wails Event "backend-error" → 前端 Events.On → showError() → ElNotification；覆盖开仓失败/挂止损失败/平仓失败/孤儿仓位补挂失败 | 后台 goroutine 错误无法通过前端调用链触达弹窗 |
| 2026-07-30 | **策略升级（借鉴 ea-03）**：① 做空支持（EnableShort 开关，OpenShort/CloseShort，止损/跟踪/PnL 全链路镜像）；② 3 分钟短窗口二次确认（RecentGainPct，默认 1.5% 阈值）；③ 1.5 倍放量确认（RecentVolumeSurge，PricePoint 新增 Volume 字段）；④ 跟踪止损参数调优（激活 10%→3%，回撤 5%→2%）；⑤ 动态上移止损价（UpdateStopMarketPrice 撤旧挂新，变动>0.1% 才触发）；⑥ 候选数量放开（TopN=0 不限制）；⑦ 前端新增 4 个配置项（做空开关/确认窗口/确认阈值/放量倍数） | 借鉴 ea-03 高胜率策略：双向交易、更早锁利、放量确认、动态止损 |
| 2026-08-02 | **修复 GetAlgoOrderStatus 持续 EOF 失败**（internal/binance/rest.go）：查询条件单状态增加瞬时错误自动重试（最多 3 次，间隔 0.5s/1s，复用 isTransientErr 判断，仅网络层错误重试、业务错误如 -2011 不重试） | 实测日志：代理/交易所连接中断（unexpected EOF）时单次失败即放弃，同一条件单（BTRUSDT/HYPERUSDT 等）每 Tick 反复查询失败达数小时；重试换新连接后大概率恢复 |
| 2026-08-02 | **修复市价平仓 -4131 拒绝导致止损失效**（COTI 案例延误 50 分钟亏损 17.34）：① rest.go 新增 CloseLongWithMark/CloseShortWithMark（LIMIT+GTC+ReduceOnly 按标记价平仓，标记价为 PERCENT_PRICE 基准必然通过过滤）+ ErrCloseLimitPending 防重复挂单（查询交易所已有平仓挂单则跳过）；② manager.go 新增 PlaceCloseLimitOrder 登记委托记录，成交后由 SyncOrders→handleFilledOrder 自动关仓（reason=LIMIT_STOP）；③ engine.go closePosition 遇 -4131 自动降级 LIMIT 挂单平仓 | 币安官方确认：-4131 为 PERCENT_PRICE filter 对市价单的成交价保护，测试网薄盘/高波动币（COTI/BANK/IOST 等）下对手价偏离标记价超限即拒绝，官方建议改用 LIMIT 单 |
| 2026-08-02 | **选币策略改为双条件筛选（15m + 24h 涨幅同时达标）**：① types.go StrategyConfig 新增 Min24hGainPct（默认 5.0%），MinGainPct 默认 10%→5%；② screener.go ScreenSliding 新增 min24hGainPct 参数，方向判定后过滤：做多要求 24h 涨幅 >= 阈值、做空要求 24h 跌幅 >= 阈值（min24hGainPct>0 时启用，0 关闭保持旧行为）；③ engine.go 调用处传入 e.cfg.Min24hGainPct；④ 前端新增"24h最小涨幅(%)"配置项；⑤ 新增 3 个测试（24h 过滤/双条件端到端/做空 24h 跌幅过滤），全部 6 包测试通过 | 用户需求：只有同时满足 24h 涨幅 >=5% 且 15m 涨幅 >=5% 的资产才入选，避免 15m 单窗口假突破 |
| 2026-08-02 | **激进版策略配置 + 熔断器真正生效**：① types.go 默认配置改为激进参数（5m 窗口、15s 扫描、双 3% 门槛、5 万成交额、TopN=8、20x 杠杆、30U 保证金、20 分钟冷却、开启做空、6% 止损、2% 跟踪激活、1.5% 跟踪回撤、2 分钟确认窗口/1% 阈值/1.8 倍放量、日亏 5%、回撤 15%）；② engine.go 接入熔断器（SetBreaker 注入、openPositions 开仓前检查 IsBlocked、closePosition 平仓后 updateBreaker 更新日亏/账户回撤、runOnce 每 Tick checkBreakerReset 跨天重置日熔断）；③ service.go 按配置创建熔断器（百分比转小数）并注入引擎 + 启动时设置初始权益；④ 前端启用日损/回撤输入框（去掉 disabled）、默认值 5/15；⑤ 新增 3 个熔断接入测试，全部 6 包测试通过 | 用户选用激进版配置并要求"日亏 5% / 回撤 15% 熔断生效"；核实后发现熔断器此前仅创建未接入引擎（预留字段不生效），本轮实现真正接入 |
| 2026-08-03 | **激进版实测排障：止损 6%→4% + -2027/-4028 开仓失败修复**：① types.go 默认止损 0.06→0.04（前端同步 6→4）；② engine.go openOne 遇 -4028（该币种不支持配置杠杆，如低价小币最高仅 10x）直接跳过该币并进入失败冷却，不再按错误杠杆静默开仓；③ openOne 遇 -2027（仓位张数超限，测试网低价小币张数上限极小如 <1000 张）数量减半重试一次，成功则按实际成交数量入库/挂单；④ 相关测试断言更新，6 包测试全通过 | 实测日志反复出现 -2027（BICO/BSB 仓位超限）、-2019 保证金不足、-2023 账户强平模式：诊断确认 ① 账户实际余额 5.6 万 U 资金充足；② 20x 杠杆 + 逐仓 30U 爆仓线 5% 早于止损 6%，插针即爆仓进入强平模式（HOMEUSDT 实测爆仓亏损 66.61U）；③ BICO 实测不支持 20x（-4028）、张数上限 <1000 张且名义价值低于 5U 下限（-4164），激进仓位在其上无法实现只能跳过；ALGO（7030 张）等正常币不受影响 |
| 2026-08-03 | **信号模式升级：K 线实体实时确认（借鉴 aoooFreqtrade）**：① StrategyConfig 新增 SignalMode（kline=15m K 线实体实时检测 / sliding=滑动窗口，默认 kline）与 MaxPullbackPct（山顶过滤器，默认 9%）；② screener.go ScreenSliding 支持 kline 模式：当前价 vs 本周期 K 线开盘价算实体涨幅（不等收线、追高最小化），sliding 模式保留可切换对比，两模式共用山顶过滤器；③ rest.go 新增 GetKlineOpen（拉当前未收盘 K 线开盘价，DRY_RUN 返回固定 100）；④ engine.go 新增 buildKlineOpenMap：24h+成交额粗筛后只对少量币拉 K 线，开盘价周期内不变缓存复用；⑤ Ticker 新增 HighPrice/LowPrice；⑥ 止盈改为 3% 激活 + 2% 回调（TrailingActivation 0.03/TrailingCallback 0.02），做空镜像，由交易所 TRAILING_STOP_MARKET 承载；⑦ 默认配置 Timeframe=15m、双 5% 门槛、关闭旧二次确认；⑧ 前端新增信号模式开关与山顶过滤器输入框；⑨ 新增 kline 模式测试 3 个 + GetKlineOpen/buildKlineOpenMap 测试，6 包测试 + 前端构建全通过 | 用户需求：替换 5m 滑动窗口信号（插针假信号多），采用「24h 涨 5% + 当前 15m K 线相对开盘价涨 5%」实时确认信号，加"别买在山顶"过滤器（距 24h 最高/最低回撤超 9% 不追），止盈改为涨 3% 激活移动止损、跌 2% 卖出 |
| 2026-08-03 | **幽灵持仓根因修复（双套止损重复触发 + -2022 误判）**：① engine.go monitorPositions 新增双触发防护：持仓已有活跃止损条件单（NEW/PARTIALLY_FILLED）时跳过本地平仓，完全交由交易所条件单 + SyncOrders 闭环（SyncOrders 每 Tick 检测 FILLED → handleFilledOrder 关仓，延迟最多 10 秒），本地 monitor 仅作条件单缺失时的兜底；② order/manager.go rollbackPosition 识别 -2022（ReduceOnly 无仓可平 = 平仓等价成功）：直接标记 GHOST 关闭并取消委托，不再误报"回滚平仓失败"保留 OPEN 等待孤儿扫描（消除数 Tick 不一致窗口与误导日志）；③ 新增 TestMonitor_SkipWhenActiveStopOrders 防护测试；④ 3 个 E2E 测试适配为"条件单失效后本地兜底平仓"场景，6 包测试全通过 | 日志反复出现"幽灵持仓已清理"与"回滚平仓失败 -2022"：根因一是价格触及止损时交易所条件单与本地 monitorPositions 双套止损几乎同时触发，交易所先成交后本地 ReduceOnly 平仓必遭 -2022；根因二是回滚路径把 -2022（无仓可平）误判为平仓失败而保留 OPEN。币安官方确认 -2022 = 该方向无可减持仓（dev.binance.vision） |
| 2026-08-03 | **开仓/平仓失败分级冷却，杜绝同类错误周期性刷屏**：① binance/rest.go 新增 APIErrorCode 错误码提取函数；② engine.go 新增结构性失败错误码集合 structuralOpenErrors（-2027/-4028/-4164/-2019/-4131），命中即拉黑该币种 12 小时（openBlockedDuration）且不弹前端窗，瞬时失败保持 5 分钟短冷却+弹窗；③ openPositions 候选循环检查 openBlocked 拉黑到期自动恢复；④ 平仓失败（如 -2023 强平模式）新增 closeRetry 3 分钟重试间隔（closeRetryInterval），monitorPositions 平仓前检查，避免强平模式解除前每 Tick 刷屏；⑤ -4028 分支从 5 分钟冷却改为 12 小时拉黑；⑥ 新增 TestOpenPositions_BlockedSymbol / TestOpenPositions_BlockedExpired 拉黑机制测试，6 包测试全通过 | 用户强烈反馈"同类后台失败反复出现"：根因是结构性失败（币种不支持配置杠杆/张数上限极小/强平模式）短期不可能恢复，原 5 分钟冷却到期后周期性重试刷屏且每次弹窗；分级冷却后每币每日最多失败 1 次、结构性失败不再打扰前端 |
| 2026-08-03 | **放量确认启用（kline 默认模式生效）+ RecentVolumeSurge 边界 bug 修复**：① 经与用户讨论确认放量倍数 1.8x、确认窗口 2 分钟（匹配 15m K 线 5% 实体信号：1.5x 偏松滤不干净、2.0x 在测试网小币上偏严、1.8x 最均衡）；② sliding_window.go 修复 RecentVolumeSurge 边界 bug：窗口起点累计成交额原取「序列最旧点」（导致放量倍数虚高 ~6.5 倍、放量过滤永远放行），改为取「最后一个 ts<=cutoff 的采样点」（正确语义，稳态成交量 surge≈1.0，放量 3 倍 surge≈3.0）；③ screener.go 放量确认从「仅 sliding 模式」放开为两种模式均生效（kline 模式滑动窗口始终在采样，volume 数据齐全）；④ engine.go kline 模式不再强制 confirmWindowMs=0（供放量确认使用，价格二次确认 ConfirmThreshold 仍由 !klineMode 守卫关闭）；⑤ types.go 默认配置 ConfirmWindowMin=2.0、VolumeSurgeThreshold=1.8（ConfirmThreshold 保持 0），前端默认值与 loadConfig 兜底同步；⑥ TestRecentVolumeSurge 期望值修正（6.31→3.0）+ 新增 kline+放量确认测试 2 个（放量达标入选 / 放量不足过滤），6 包测试全通过 | 用户讨论"放量多少适合匹配策略"：此前 kline 模式强制关闭放量确认（confirmWindowMs=0 + !klineMode 守卫），放量配置形同虚设；且底层 RecentVolumeSurge 边界 bug 使放大倍数虚高，即使放开也永远不会过滤。本轮讨论定参并修复后，放量确认在默认 kline 模式下真正生效 |
| 2026-08-03 | **默认参数对齐用户设定：10x 杠杆 / 20U 保证金 / 8% 止损**：① types.go DefaultStrategyConfig Leverage 20→10、PositionMarginUSDT 30→20、StopLossPct 0.04→0.08（10x 爆仓线约 10%，8% 止损早于爆仓且留足波动空间）；② 前端默认值同步（leverage 10 / positionMarginUsdt 20 / stopLossPct 8）；③ TestDefaultStrategyConfig 断言同步；④ 6 包测试全通过 | 用户明确要求：杠杆 10 倍、保证金 20U（此前曾声明止损 8%）。原默认 20x/30U/4% 为用户未改前的激进版遗留，现按用户设定对齐 |
| 2026-08-03 | **回测系统子集验证完成（30 主流币 2024-01~2026-07）**：① 实测数据——下载合并 814 万根 5m K 线耗时 1.2 分钟（8 并发），回测 271,584 个时间片耗时 5.3 秒；② 修复两处撮合问题——SHIB/PEPE/BONK 合约代码实为 1000 前缀（修正币种列表）；开仓当片不评估跟踪止盈（消除 85 笔 0 盈亏虚假平仓）；③ 默认参数回测结果：490 笔交易、胜率 61%、平均盈 4.01U / 亏 13.03U、PF 0.48、总收益 -128.94%、最大回撤 128.9%——**证明当前 8% 止损 + 3%/2% 跟踪的参数盈亏结构不成立**（盈利单被 2% 回调过早打掉，亏损单每笔 16U）；④ aooo 宽跟踪对比（10% 止损 + 10%/5% 跟踪）：413 笔、胜率 50%、平均盈 16.3U / 亏 19.75U、PF 0.83、总收益 -69.37%——宽跟踪显著优于窄跟踪，引擎能正确区分参数优劣（无系统性 bug）；⑤ 新增 -sl/-act/-cb/-surge/-only-long 参数 flag 支持敏感性对比；⑥ 全量下载启动（300 币，-all + 代理 127.0.0.1:7897，fapi 需代理而 data.binance.vision 可直连） | 子集验证结论：回测系统工作正常，当前实盘参数在 5m 历史数据上必然大亏，与前期"激进参数=高频噪音交易"判断一致；宽跟踪止盈（aooo 风格）可显著改善，但单独调参仍不足以转正，需结合更强的信号过滤（收盘确认/更高涨幅门槛）或更优参数组合 |
| 2026-08-03 | **新信号范式回测（均值回归 + 趋势跟随，全量 6 组）**：引擎新增 `-mode` 三范式支持——mr（5m 实体暴跌≥3% 接反弹，反弹 +2~3% 止盈/再跌 1.5~2% 止损/2h 超时平仓，距 24h 高回撤 [2~15%] 过滤）、trend（EMA(96,288)≈8h/24h 金叉死叉，反向交叉平仓 + 可选跟踪止盈），共用破产保护与手续费。全量结果：M1/M2/M3（MR 三组，含只做多与更恐慌 5% 门槛）1898/1872/1907 笔、胜率 33~38%、PF 0.81~0.85、总收益 -98.2%；T1（EMA 纯交叉退出）800 笔、胜率 24.8%、平均盈 11.26/亏 5.12（盈亏比结构最优但胜率过低）、PF 0.72、-98.4%；T2（+5%/3% 跟踪）1579 笔、胜率 38%、PF 0.86、-98.1%——**四种范式家族（盘中/收盘追涨、均值回归、趋势跟随）11 组配置在 2.6 年×567 币全部爆仓**。结构性规律：追涨输在盈亏结构（盈 4~15U 亏 16~20U）、均值回归输在胜率（38% 不抵手续费）、趋势纯交叉胜率仅 25%（EMA 交叉滞后+震荡市鞭打）；每种范式的"奖酬结构"缺陷各不相同，但共同点是 5m 粒度下无正期望 | 用户选定「试新信号范式」：均值回归与趋势跟随在诚实口径（破产保护+手续费）下同样不可存活。MR 的奖酬比正确（盈 3.9U>亏 3.0U）但 38% 胜率不足；TREND 奖酬比最大（2.2:1）但 25% 胜率不足。已测试矩阵：11 组配置×4 范式家族 |
| 2026-08-03 | **资金费率套利回测（第 5 种范式家族，全量 11 组）**：① 新增 `cmd/download-funding` 下载器（data.binance.vision fundingRate 月度 zip，2024-01~2026-07 全 567 币、172.6 万条资金费率记录，总耗时 2.2 分钟 / 92MB；新版 CSV 为 calc_time/funding_interval_hours/last_funding_rate 三列，calc_time 带毫秒偏移需归一 8h 边界）；② 引擎新增 `-mode funding`：费率 <=-阈值做多收取 / >=+阈值做空收取（正费率=多头付空头，多头收取=-rate×名义值），费率回归平仓 FUND_EXIT / 最长持有周期平仓 FUND_TIME / 价格止损 FUND_SL，资金费收入单列报告；③ 全量 11 组矩阵（阈值 0.01%~0.2% × 持有 1~6 期 × 止损 5/10% × 仓位 5/10）——**资金费收入真实为正且随阈值单调放大（0.01%→+470U … 0.2%→+2460U），但单腿价格敞口亏损恒大于资金费收入，11 组全部爆仓 -98%~-99%（PF 0.67~0.99，胜率 24~47%）**；④ 高阈值组 85~95% 为多头（深度负费率=逼空顶，做多接盘）胜率降至 24~30%、止损占 59%；⑤ 结构性结论：**资金费率套利的收取端确实有效（+470U~+2460U 可复现），但其成立必要条件是 Δ 中性（现货或跨所对冲）；单腿永续执行下，极端费率币 8~24h 价格波动（±5~10%）永远吞没资金费收入——第 5 种范式家族在现有执行框架内同样不可存活** | 用户选定「资金费率套利」：唯一不与方向性信号同类的市场中性范式。收取端为正证明套利边真实存在，但无法在单腿执行框架落地；引擎验证：不同范式呈现不同且正当的盈亏结构（本次为「收入正/价格端必亏」），无系统性 bug。f9/f10/f11 阈值语义下均退化为「首期即平」，结果完全一致（2820 笔 +988U）反证退出时点无关性 |
| 2026-08-04 | **S01 纯追涨（无门控）锁定并落地实盘**：① types.go StrategyConfig 新增 TakeProfitPct（固定止盈，默认 0.10）与 MaxHoldMin（最长持仓分钟，默认 120），补齐回测口径两项退出机制；② DefaultStrategyConfig 全面对齐 S01 回测参数（回测口径 `-mode momentum -closed=false -exit-mode=close -topn 10 -maxpos 5 -minvol 100000 -cooldown 60 -gain 4 -min24gain 4 -surge 1.5 -sl 6 -tp 10 -act 3 -cb 2 -hold 120 -margin 10 -lev 10 -only-long`，三年 +4449U/+445%/回撤 12.3%）：MinGainPct/Min24hGainPct 5→4、MinQuoteVolume 50000→100000、TopN 8→10、PositionMarginUSDT 20→10、CooldownMin 20→60、StopLossPct 0.08→0.06、VolumeSurgeThreshold 1.8→1.5、EnableShort true→false（纯多）；③ rest.go 新增 PlaceTakeProfit（Algo Order API TAKE_PROFIT_MARKET + ClosePosition + MARK_PRICE，DRY_RUN 模拟）；④ manager.go PlaceStopOrders 挂止损/跟踪后追加挂固定止盈单（失败不触发回滚仅告警，触发价复用 StopPrice 字段入库），handleFilledOrder 新增 TAKE_PROFIT 原因映射，平仓日志文案通用化；⑤ engine.go monitorPositions 在条件单检查之前新增 MaxHold 超时平仓（reason=MAX_HOLD，先撤委托再市价平仓）；⑥ 前端 strategy/index.vue 默认值同步 + 新增固定止盈/最长持仓两项配置输入；⑦ 测试更新（binance/bindings 默认值断言 + PlaceTakeProfit DRY_RUN），6 包全通过 | 用户正式锁定 S01 纯追涨（无门控）为唯一实盘策略：EMA 门控回测验证为负优化（+3236U vs S01 +4449U），ATR 阈值与门控均不采用。此前实盘缺固定止盈与最长持仓两项退出逻辑无法 1:1 复刻 S01，本轮补齐后可完全按 S01 口径开始跑模拟盘 |
| 2026-08-04 | **回测模块并入主模块，消除 gopls 嵌套模块诊断**：① 删除 backtest/go.mod（嵌套独立模块）与根目录 go.work，backtest 成为 quant-desktop 主模块的一部分（纯标准库、无包间依赖，编译零影响）；② 验证：根目录 `go build ./...` 与 backtest 目录 `go build ./cmd/backtest ./cmd/download ./cmd/download-funding` 全部 EXIT=0；③ 回测运行命令不变（`cd backtest && go run ./cmd/backtest -data ../data -out ../out`） | IDE gopls 对嵌套模块存在缓冲区 bug：gopls 用编辑器空缓冲区（overlay）覆盖磁盘上有效的 backtest/go.mod，导致 "missing module declaration" 错误扩散到全项目；已用 `go list -overlay` 实验精确复现（空 overlay 必现、正确 overlay 正常）。删除嵌套模块后 gopls 自动重载，Error/Warning 全部清零，仅余 Hint 级可选风格建议 |
| 2026-08-04 | **固定止盈改为纯跟踪（TakeProfitPct 0.10→0）+ 最大持仓 5→10**：① types.go 默认值 0.10→0（注释记录变更原因），manager.go 已有 `TakeProfitPct>0` 守卫，置 0 自动跳过 TAKE_PROFIT_MARKET 挂单；② MaxOpenPositions 5→10（types.go + 前端默认值同步）；③ 前端 strategy/index.vue 默认值/兜底同步；④ 3 处测试断言更新（binance×2 + bindings×1）；⑤ 验证：go build + go vet + 3 包 `-race` 全过，前端 pnpm build 4.79s；⑥ 重启模拟盘生效 | 用户否决 S01 的 10% 固定止盈设计：10% 到点即平，截断 3% 激活的移动止盈在 10% 之后的利润奔跑空间；随后用户要求最大持仓 5→10。两项均偏离 S01 回测口径（基线 -tp 10 -maxpos 5 的 +4449U/回撤 12.3%），未回测，模拟盘实盘验证中；另提出"移动止盈激活时追加仓位"需求，待回测验证（-addon 变体） |
| 2026-08-04 | **二次全量复查 + 修复 3 项（#1 major/#2 major/#3 minor）**：① main.go 运行失败路径先显式 `quantService.Shutdown()` 再 `log.Fatalf`（os.Exit 不触发 defer，避免 Run 错误时资源未清理）+ L19/L59 错误日志统一为 `log.Fatalf("中文: %v")`；② service.go `Shutdown()` 全程持有 `s.mu` 串行化 db/engine 字段访问（消除并发/双重调用下 s.db 数据竞争，-race 可证），新增 `engineWG sync.WaitGroup` 汇合引擎 goroutine（engine.Stop → wg.Wait → db.Close 顺序，消除 db 关闭与 runOnce 竞态）；③ 8 个 db 读取方法补锁 + nil 防护（GetSavedCredentials/GetDashboardData/GetClosedPositions/GetPositions/GetLogs/GetOrders/GetOrderEvents 加 RLock、CancelOrder 加 Lock，nil 时返回"服务已关闭"或空数据）；④ 验证：`go build` + `go vet ./internal/...` + `go test -race ./internal/bindings/ ./internal/binance/` 全部通过。详见「代码审查记录-2026-08-04.md」 | 复查（2 子代理交叉验证）确认：① log.Fatalf 内部 os.Exit(1) 不执行 defer，与"资源清理统一由 defer 执行"注释矛盾（仅 Run 失败路径可达，Init 失败路径无资源可泄漏）；② Shutdown() 无锁，并发调用/与业务方法并发时 s.db 读-写存在数据竞争；③ Shutdown 不等待引擎 goroutine，db.Close 可能与运行中 runOnce 的 DB 写入竞态。另确认原审查建议 "could use tagged switch" 为误报（main.go 无类型断言链，2/2 代理一致） |
| 2026-08-04 | **追加仓位功能实现（直接上引擎，未回测）**：① StrategyConfig 新增 EnableAddOn（默认 true，前端开关同步）；② engine.go openPositions 将「持仓币一律排除」改为「追加仓判定」：持仓币开启 EnableAddOn 且现价 >= 首仓入场价*(1+TrailingActivation)（移动止盈已激活）且再次命中信号（候选已含该币=通过 24h/15m K 线/放量/山顶过滤）且方向一致（防对冲）且同币持仓数 < 1+maxAddOnsPerSymbol（每币最多追加 1 次，单币最多 2 仓），允许追加 1 张独立新单——独立入场价/独立 6% 止损/独立 3% 激活+2% 回调移动止盈/独立 120 分钟超时/独立占用槽位；③ 首仓入场价基准：持仓按 opened_at DESC 返回（最新在前），做多取同币最低入场价、做空取最高（追加仓必然在更差价格，取极值即原仓）；④ 新增 4 个测试（激活追加/未激活不加/上限 2 仓不加/开关关闭不加），6 包 `-race` 全过 + 前端构建 4.62s；⑤ 重启模拟盘生效 | 用户选定「直接上引擎 + 每币追加 1 次」：移动止盈激活（+3%）时持仓币再次命中信号可追加入场，解决 10% 固定止盈封顶后「利润奔跑+加仓」需求（固定止盈已关闭为纯跟踪）。结构性新行为未回测（-addon 变体仍为待办），模拟盘实盘验证中 |
| 2026-08-04 | **单实例锁 + 运行规范定调（根治多窗口）**：① main.go 新增 `acquireSingleInstanceLock()`：临时目录锁文件存 PID，重复启动直接退出（提示"已在运行 PID xxx"），崩溃残留锁自动抢占重建；② 启动唯一方式定为 `go build . && ./quant-desktop`，禁用 `go run .`（孤儿进程）与 `wails3 dev`（仅限前端开发）；③ 验证：实例 2 启动被拦截退出码 1，当前仅 1 个进程；④ 文档新增 §3.6 运行规范 | 用户反馈 3 个窗口同时开着（exec/quant-desktop/exec）：go run 孤儿进程 + wails3 dev 实例 + 生产二进制三实例共存，且并发写同一 simulation 库。已清理残留并加代码层硬保证，多开不可能再现 |
| 2026-08-04 | **「应用到项目」按钮：策略配置持久化**：① service.go 新增 `PersistConfig`：整份 StrategyConfig JSON 序列化存入 strategy_config 表的 `strategy:cfg` 键（复用 GetKeyValue/SetKeyValue，按当前模式库天然隔离 SIMULATION/LIVE），运行中也允许保存并提示"下次启动生效"；② Init 启动时自动加载持久化配置覆盖默认值（库无记录时用 S01 默认）；③ 前端策略页新增「应用到项目」按钮（warning 色，保存当前表单配置）；④ 新增 2 个测试（TestPersistConfig_Roundtrip 写库回读一致 / TestPersistConfig_Running 运行中保存提示），6 包测试全过 + 前端 pnpm build 4.97s | 用户要求：临时改参数（点「启动策略」内存生效、重启丢）之外，增加「应用到项目」将参数写入数据库，应用重启后由 Init 自动加载、配置长期保留 |
| 2026-08-05 | **模拟盘 10.5 小时实测：追加仓位全链路验证通过**：① 52 笔平仓 +85.69U、胜率 75%（TRAILING_STOP 34 笔 +126.66 为利润核心，STOP_LOSS 5 笔 -29.70 及时截断，MAX_HOLD 12 笔 -11.27 磨单）；② 追加仓成功触发 10+ 次（AGT/BICO/HEI/SKYAI/TAKE/JCT 等），成对获利平仓（如 HEI 双仓 +6.52/+10.15、SKYAI 双仓 +2.57/+7.74）；③ HFT 单币 20 笔 +49.69U（80% 胜率），两波追加仓对贡献 73% 盈利；④ 平仓失败（MAX_HOLD EOF）自动 3 分钟重试闭环验证（TAKEUSDT 12:33 重试成功平仓）；⑤ 已知问题：条件单状态查询 demo API 网络超时（需网络容错加固）、止损滑点（AGT -9.47U 超理论 -6U） | 模拟盘持续运行验证追加仓位功能与 S01 改动版（纯跟踪/10 仓/追加仓）真实表现：单日正收益 + 高胜率亮眼，但未回测，仍需 -addon 回测变体验证盈利性后才能评估实盘能力 |
| 2026-08-05 | **代理候选链配置（本地 10808 优先 + 远端 45.251.241.89 兜底）**：① rest.go NewClient 代理解析升级为候选链：用户指定 → 内置链（127.0.0.1:10808 500ms / 45.251.241.89:49988 1.5s）→ 本地自动检测（7897 等）→ 直连，每级 TCP 探测不可达自动跳过；② service.go 默认代理改为 127.0.0.1:10808（库中无保存值时使用，Windows 新库开箱即用）；③ 验证：go build + binance/bindings 测试通过；④ 说明：两个代理从开发机实测暂不可达（10808 本机无监听、45.251.241.89 超时），已加探测回退保护不写死 | 用户要求"写入系统，以后 Win 上就能用"：应用不读系统代理设置，改为内置候选链 + 探测回退，新环境自动尝试用户提供的两个代理 |
| 2026-08-05 | **Windows 打包完成（exe 16MB，Zig 直编译替代 Docker）**：① 原 Docker + wails-cross 方案因 Docker Desktop 故障（daemon 500/VM 不可达/无法拉起）弃用，改为 Zig 本机交叉编译（`brew install zig`，同款 zcc 包装脚本，与 wails-cross 镜像内部逻辑一致）：`CGO_ENABLED=1 GOOS=windows GOARCH=amd64 CC=scripts/zcc-windows-amd64 go build -ldflags="-w -s -H windowsgui"`；② 包装脚本解决 3 个坑：剥离 `-target` 避免冲突、`-mwindows` 翻译为 `-Wl,--subsystem,windows`（否则 exe 是 console 弹黑窗）、`-fno-sanitize=all`（zig 0.16 默认 UBSan 报 undefined symbol）；③ 验证全过：`file` 显示 PE32+ (GUI) x86-64、subsystem=0x2、元数据 UTF-16 齐全（量化交易客户端/QuantDesk/1.0.0/币安合约量化交易桌面客户端）、exe 内 backtest/data_funding 零引用、SQLite 已链接（484 符号）；④ 脚本 build-windows.sh 重构（默认 Zig，docker/native 备选），文档 §3.7 全量更新为 Zig 方案 | 用户要求：打包 Windows 可执行文件，配置忽略本地历史数据文件夹，含元数据，提供步骤文档，Win10+ 稳定运行；实测 Docker 方案不可用，换 Zig 后一次成型 |
| 2026-08-05 | **修复代理探测误选"死代理"**（internal/binance/rest.go）：① 探测从纯 TCP 握手升级为 TCP + HTTP 实测（通过代理请求币安 `/fapi/v1/time`，200 才算可用）——原逻辑下端口通但无法转发 HTTPS 的代理（45.251.241.89:49988）被误选，导致所有账户请求 unexpected EOF/超时；② 顺序调整为「用户指定 → 本机自动检测(7897/7890/10808…) → 远端兜底」本地优先；③ 修复测试 URL 选错（`/api/v1/time` 在 fapi 域名不存在返回 CloudFront 403，误判可用代理为不可用，改用 `/fapi/v1/time`）；④ 实测 127.0.0.1:7897（Clash）通过，程序正确选中；⑤ Windows/mac 重新打包 | 用户 Win10 实盘遇 unexpected EOF；实测本机 7897 代理可用而程序误选远端死代理；另确认用户 Clash 代理访问 fapi.binance.com 实测 200，实盘网络链路通 |
| 2026-08-05 | **代理支持 SOCKS5 协议**（internal/binance/rest.go + go.mod）：① 探测函数 `probeProxy` 替代 `verifyProxy`，对每个候选依次实测 HTTP 与 SOCKS5 两种协议，返回带正确 scheme 的 URL（socks5://127.0.0.1:10808）；② `newProxiedHTTPClient` 按 scheme 分流：http/https 走标准 `http.ProxyURL`，socks5 走 `golang.org/x/net/proxy` 的 SOCKS5 拨号器（`socks5Dialer` 适配 `Transport.DialContext`，优先用 ContextDialer 支持取消）；③ WS 部分无需改代码：gorilla/websocket v1.5.3 内部 `proxy_FromURL` 原生支持 socks5 scheme，`SetWsProxyUrl("socks5://...")` 直接生效；④ 依赖新增 `golang.org/x/net v0.57.0`；⑤ 验证：go build + binance/bindings 测试全过，exe 重新打包 16MB，strings 确认 socks5 拨号逻辑已编译进 exe、历史数据零引用、元数据齐全 | 用户 Win10 实盘用 v2rayN（默认 10808 为 Socks 端口），程序原仅支持 HTTP 代理导致连不上本地代理；改为双协议实测+连接，v2rayN 默认配置开箱即用 |
| 2026-08-05 | **「测试连接」按钮内置到 exe（完整认证诊断链路）**（internal/binance/rest.go + bindings/service.go + frontend/strategy/index.vue）：① rest.go 新增 `TestConnection`（标准 HMAC-SHA256 签名请求 `/fapi/v1/positionSide/dual`，只读绝不下单，直接读取币安原始响应）；② **诊断升级**：Client 新增 `proxyURL` 字段记录实际选中代理；TestConnection 输出 5 项诊断——`domain`（实际请求域名 fapi=实盘 / demo-fapi=测试网，防止误发测试网）、`proxy`（代理链路：用户指定/自动检测/直连+协议）、`network`（无签名公开接口 `/fapi/v1/time` 自检，区分"网络/代理问题"与"Key 问题"）、`exit_ip`（经同一代理链请求 `api.ipify.org` 获取出口公网 IP，供与币安 IP 白名单对比）、`message`（成功摘要含余额 / 失败解析 code/msg/request ip）；③ 前端失败弹窗用 ElNotification+HTML `<br/>` 逐行展示完整诊断，出口 IP 行附"请与币安 IP 白名单核对"提示；④ 新增 3 个测试（DRY_RUN/无凭据/Nil 客户端），6 包测试 + 前端构建全过；⑤ 删除临时诊断文件 live_probe_test.go（功能已由内置按钮替代）；⑥ exe 重新打包 16MB，二进制搜索确认前后端诊断词全部编译进 exe | 用户要求"做一个测试的程序放在 exe 里拿到 Win 上测一遍"：同 Key 在 mac 实测有效、Win 上 -2015 且无 request ip，原版只返回 ok/message 无法定位；升级后 Win 上一键输出域名/代理/出口 IP/币安错误，可区分白名单未放行、Secret 填错、请求发错域名三类根因 |
| 2026-08-05 | **P0 严重修复：全局 UseDemo 残留导致实盘误发测试网（-2015 根因）**（internal/binance/rest.go）：① NewClient 增加 else 分支——非 SIMULATION 模式显式 `futures.UseDemo = false`；② 根因：`futures.UseDemo` 是 go-binance 包级全局变量，用户先切模拟盘（设 true）再切回实盘时，LIVE 分支不重置，REST 与 WS（getWsEndpoint 同样读 UseDemo）全部残留指向 `demo-fapi.binance.com`，实盘 Key 在测试网返回 -2015（Invalid API-key）且无 request ip；③ 新增回归测试 TestNewClient_Live_ResetsDemoFlag（模拟"先模拟盘后实盘"，断言 UseDemo 重置且 BaseURL=`fapi.binance.com`）；④ 修复前该测试必失败（BaseURL 残留 demo-fapi），修复后通过；⑤ 6 包测试全过 + exe 重新打包 16MB，二进制确认 fapi/demo-fapi 双域名与诊断 UI 均编译进去 | Windows 实盘「测试连接」诊断暴露关键行「请求域名：`https://demo-fapi.binance.com`」——用户选的是实盘 LIVE 却发到测试网；这同时解释了全部现象：模拟盘正常（demo 正常）、mac 上 fapi 成功（mac 首次启动 UseDemo=false）、Win 上 -2015 无 request ip（测试网不认识实盘 Key）。修复后实盘请求回到 `fapi.binance.com` |
| 2026-08-05 | **修复 IDE gopls 对 embed 的误报（SA9009 + "no matching files found"）**（main.go）：① main.go 的 embed 指令由 `//go:embed all:frontend/dist` 改为 `//go:embed frontend/dist/*`——二者编译行为完全等价（dist 目录无隐藏文件，`all:` 前缀仅影响 `.`/`_` 开头文件的匹配），但 **gopls 对 `all:` 前缀的目录 embed 模式存在误报 bug**（持续报 "pattern all:frontend/dist: no matching files found"，且 main.go 内容变更触发重载后仍报、注释 .gitignore 后仍报、命令行 go list/go build 均正常），去掉 `all:` 后诊断立即消失；② 同时修正 main.go 顶部注释：`// go:embed ...`（带空格）会被静态检查 SA9009 判定为无效编译器指令，改为普通中文注释；③ 验证：go build + vet + binance/bindings 测试全过，exe 重新打包 16MB（embed 产物与 all: 写法一致），诊断面板清零 | IDE「问题」面板持续显示 main.go embed 报错，用户两次反馈诊断；经排查确认为 gopls 对 `all:` 目录 embed 模式误报（非代码问题），改为等价写法规避 |
| 2026-08-07 | **新增新币过滤功能**：① binance/rest.go：LoadExchangeInfo 顺带缓存 `exchangeInfo.onboardDate`（onboardDateMap，symbol→Unix 毫秒），新增 GetOnboardDate 读取（未知返回 ok=false）；② strategy/new_listing.go 纯函数 ListingDays（上市天数=自然日向下取整，异常/未知返回 -1）+ filterTickers（仅剔除传入筛选器的列表，priceMap/滑窗保留全量）；③ engine.go：runOnce 每 Tick 计算 buildNewListingBlocked 拦截集合（过滤日志写入 trade_logs，module=screener，含上市日期与天数，newListLogged 每合约每进程去重一次），buildKlineOpenMap 对被拦截合约跳过 K 线拉取，筛选层 filterTickers 剔除，openPositions 增加 isNewListing 防御检查（含追加仓路径）；④ types.go StrategyConfig 新增 enableNewListingFilter（默认 true）+ newListingMinDays（默认 60），持久化可调；⑤ bindings/service.go Init 旧持久化配置缺失新键时自动迁移默认值（用户显式保存的 false/0 不覆盖）；⑥ 前端策略页新增「新币过滤」开关与「过滤天数(天)」表单项；⑦ 双层防护 + 失败放行（onboardDate 未知不拦截，与 IsFuturesSymbol 一致）；平仓/监控/委托同步路径不经拦截点，不影响已有持仓 | 用户需求：排除上市时间 ≤60 天的新币永续合约（无历史数据、波动剧烈、追涨风险高），防止新币被追涨；不影响其他正常合约交易流程 |
| 2026-08-07 | **最小成交额 10 万 → 1000 万 USDT**：① types.go DefaultStrategyConfig.MinQuoteVolume 100000→10000000；② bindings/service.go 抽离 migratePersistedStrategyConfig：旧持久化配置（含新币过滤缺键补默认 + 旧成交额 100000 精确匹配）启动自动迁移为 10000000 并回写 DB，用户显式保存的其他值不覆盖；③ 前端策略页默认值同步 1000 万（loadConfig 直接透传后端值）；④ 新增迁移测试 4 个（旧值迁移/新值不迁移/非默认不覆盖/非法 JSON 报错）+ rest_test 默认值断言更新；⑤ 回测引擎 MinQuoteVolume 默认 5 万独立于交易系统，未受影响 | 用户需求：过滤小市值低流动性币，24h 累计成交额门槛从 10 万提高到 1000 万 USDT |
| 2026-08-14 | **统一 3 桶口径：报表/漏斗/机会价值全部改为「5m 收盘 vs 前一根收盘」**（tools/market_metrics/main.go + 前端文案）：① 定位到口径不一致——策略（客户端 maxGain5mFromKlines + 回测 max5mGain）用「5m 收盘 vs 前一根收盘」分桶（≥2.5% 爆拉×1.5 / 2%~2.5% 中间×1.0 / <2% 温和×0.7），而报表工具 max5mEntity 用「5m 收盘 vs 本根开盘」实体涨幅，导致同一机会报表标桶与客户端实际仓位倍数对不上；② main.go 新增 max5mGainClose（与客户端 maxGain5mFromKlines 逐行同口径：周期内首根以周期前一根收盘为基准，取周期内最大收盘涨幅），替换全部 max5mEntity 调用点（机会模拟分桶/实际成交回放桶/拦截健康度/机会价值/逐单明细），collect 的 5m 爆拉计数同步改为收盘 vs 前一根收盘；③ analyzeBurst（--raw）爆拉判定同步改口径；④ 文案全对齐：main.go 汇总说明、前端 summary/strategy/strategy-summary 页的"5m单根涨"全部改为明确的"5m 收盘涨幅（这根5m收盘 vs 前一根5m收盘）"，同时修正两处过时注释（模拟已按桶放大仓位却写"未放大"）；⑤ 新增 --date 参数支持按历史日期重跑分析（含实际成交/尝试/心跳按自然日上下界过滤），重跑 2026-08-13 全口径数据并覆盖旧 CSV/daily_summaries；⑥ 新增 bucket_test.go 3 个单测（与客户端 maxGain5mFromKlines 同款数据断言一致），工具测试 + 前端构建 + D 客户端暂存版（quant-desktop-smart\quant-desktop-new.exe）全部通过 | 用户发现报表/漏斗的桶（5m 实体口径）与策略/回测的桶（5m 收盘 vs 前一根收盘）不一致：同一个机会，报表可能标爆拉桶而客户端实际只加 1.0 倍仓位，蓝卡分桶与实盘仓位对不上；要求全部统一到策略口径，数字才能说得清 |
| 2026-08-14 | **1m vs 5m 桶粒度对比实验（--bucket1m）**：① analyze 新增 --bucket1m 开关：策略逻辑/信号/进出场全部保持 5m 不变，仅 3 桶度量从「5m 收盘 vs 前一根 5m 收盘」换成「1m 收盘 vs 前一根 1m 收盘」（当前 15m 周期内截至所在 5m 收盘时刻取最大），阈值 2.5/2/0.7 保持不变以便纯粒度对比；② 新增 max1mGainClose + bucketGainAt，机会模拟/实际成交回放/拦截健康度/机会价值全链路换桶；③ 1m 版 CSV 输出带 _1m 后缀（analysis/intercept_health/opportunity_value），不覆盖标准 5m 日报、不写 daily_summaries；④ 新增 TestMax1mGainClose 单测；⑤ 当日（2026-08-14）实测对比：同样的 15 个机会、5 笔成交、33.3% 转化率不变，桶分布翻转——5m 版爆拉 10/中间 4/温和 1，1m 版温和 10/中间 1/爆拉 4（单根 1m 收盘很少一口气涨 2.5%，多数机会落入温和桶）；模拟盈亏 5m -27.75U vs 1m -22.00U（1m 桶更保守、仓位更小，单日样本仅供参考）；结论待多日样本验证 | 用户问"5m 够短还是 1m"：5m 是回测验证过的粒度；1m 更细但更吵，且阈值必须重调。先做同阈值纯粒度对照实验，用当天 1m/5m 数据跑 D 策略看开仓漏斗差异，再决定是否值得换 |
| 2026-08-14 | **A vs D 策略变体模拟（--variant A）**：① analyze 新增 --variant D|A：从 A/D 客户端持久化配置确认两者信号/成交额/止损完全一致（minGain 3%、minQuoteVolume 2000 万、止损 3%、act2/cb3/180 分钟），唯一差异是 5m 爆拉仓位（A 关闭、D 开启）；故 A 变体 = 全桶 ×1.0、D 变体 = 爆拉×1.5/中间×1.0/温和×0.7，信号与进出场完全共用，隔离出爆拉仓位因子的真实贡献；② replayForcedOpen 重构为 replayForcedOpenR(k5, j, flat, bucket)（保留 D 默认包装供测试），机会价值/拦截健康度/漏肉全链路按变体取倍数；③ CSV 带 _A 后缀、不写 daily_summaries（标准 D 日报不受影响）；④ 昨日（2026-08-13）实盘数据实测：机会 196/成交 64/转化率 32.7% 两版完全一致（信号未变），模拟盈亏 A -117.56U vs D -158.21U、机会价值 A -176.09U vs D -239.63U、盈利捕获率 A 50.9% vs D 41.3%——昨日爆拉桶整体亏钱（-211 vs -141），D 在爆拉桶×1.5 放大了亏损约 41U；单日样本说明不了因子好坏（回测三年该因子为正贡献 +36%），需多日 A/B 并跑 | 用户要求"昨天 A 和 D 策略在实盘数据下的模拟盘跑下对比"：用同一份 08-13 实盘 K 线，分别按 A 骨架（全桶×1.0）与 D 智慧版（爆拉×1.5/温和×0.7）回放同一批机会，量化爆拉仓位因子当天是帮忙还是帮倒忙 |
| 2026-08-14 | **纯模拟模式（--sim-only）：A/D 对比只读实盘 K 线，不读客户端成交**：① 新增 --sim-only：不加载客户端实际成交/开仓尝试/心跳（不依赖 --db），所有模拟开仓计为成交（100% 转化），漏斗只剩「机会 vs 策略规则拦截」两层；机会价值表的实际成交/捕获率列显示 "--"；② --variant A 自动进入纯模拟（A 无真实成交可对照）；CSV 带 _sim / _A 后缀、不写 daily_summaries；③ 修正 A/D 对比口径后重跑 08-13 实盘数据：机会 196/成交 196（纯模拟）两版一致，模拟盈亏 A -120.96U vs D -163.26U、机会价值 A -178.96U vs D -244.15U（爆拉桶 A -144.21 vs D -216.26，D ×1.5 放大爆拉桶亏损约 72U；温和桶 D ×0.7 少亏约 7U）——昨日爆拉仓位因子帮倒忙约 42U，单日样本仅供参考 | 用户指出 A/D 对比里"实际盈亏"列不应读 D 客户端真实成交（A 没有真实成交，两边显示同一数字是误导）：要求 A/D 模拟只基于下载的实盘 K 线回放，去掉执行层干扰 |
| 2026-08-14 | **D 客户端对齐回测口径（盘中实时降级为闹钟 + 爆拉桶只用已收盘 5m）**：① 根因定位——三台机器口径不一致：回测（backtest 引擎）按「已收盘 5m 收盘价 vs 15m 周期开盘 ≥3%」判定机会、下一根 5m 开盘成交、爆拉桶用已收盘收盘价、含手续费 0.05%+滑点 0.1%；D 模拟盘客户端却在每 15s 用盘中实时价触发开仓（K 线中间追尖进场）、用未收盘的实时价算爆拉桶（maxGain5mFromKlines 不过滤未收盘）、且漏斗显示当日"客户端多开 57 笔"（回测里不存在的交易）——**回测验证过的因子从没验证过这台实时客户端**；② engine.go：kline+SmartSize 模式下，实时候选（ReasonLiveSignal）仅落库作闹钟，不再进入 openPositions，开仓只走 appendKlineCloseCandidates（5m 收盘对齐通道）；③ appendKlineCloseCandidates 去掉「实时涨幅 ≥ MinGainPct-1」预筛门——收盘价可能在实时价未接近阈值时达标（收盘瞬间跳价），预筛会漏掉与回测一致的机会，改为对粗筛通过的币每根 5m 全量判定；④ rest.go maxGain5mFromKlines 排除未收盘 5m（实时价不计入爆拉桶），且周期基准改为「最新已收盘 5m」所在 15m 周期（修复 15m 边界 0~15s 窗口把刚收盘的上一周期 K 线错配到新周期的 bug）；⑤ 测试：kline_gain_test.go 修正原测试数据错位（nowMs=09:30 是 15m 边界，原测试把周期起点算成 09:30 导致数据全错位、靠"未来 K 线当已收盘"才通过），新增未收盘 K 线排除单测；internal/binance + internal/strategy 全过；⑥ D 暂存版 quant-desktop-smart\quant-desktop-new.exe 已重建，待用户退出客户端后替换部署 | 用户要求给出"3 桶回测验证通过、模拟盘却失败"的根因（不许甩锅数据）：查明是执行口径不一致——回测用已收盘 5m 判定+成交，客户端用盘中实时价追尖+未收盘实时价算桶；用户选定方案 A：盘中实时只当闹钟，开仓只走 5m 收盘通道（=回测口径） |
| 2026-08-14 | **漏斗模拟补成本与过滤器 + 回测引擎单日重放（fetchday + backtest）**：① market_metrics 模拟新增手续费 0.05%/边 + 滑点 0.1%（pnlWithCost，入场×(1+滑点)、出场×(1−滑点)、双边手续费按名义×0.05%）、24h 涨幅过滤（0.1%）与山顶过滤（距 24h 最高 9%，历史重放用 K 线滚动计算 288 根高/收盘，当日实时用行情快照）、最大持仓对齐客户端（D=20 / A=15）；② --date 历史重放修复：新增 fetchKlinesRange 按目标日期拉全量数据（前 24h 预热），此前只拉"最近 24h"导致昨天凌晨时段根本没覆盖（信号 307→84 的假象）；③ 新增 backtest/cmd/fetchday：按机会池抓取目标日 5m K 线（+24h 预热）写入回测 CSV 格式，再以 backtest 引擎 + D 配置单日重放（-start/-end 用 Unix 毫秒绕开 parseDay UTC 偏移，否则预热被"跳过起始日之前数据"丢弃 → 24h 窗口永不就绪 → 0 笔）；④ **关键发现——出场口径决定成败**：同一份 08-13 实盘数据、同一 D 配置，回测引擎 -exit-mode=close（只看收盘价，S01 锁定命令用的就是它）→ 77 笔 +146.95U；-exit-mode=ohlc（片内高低价，=客户端 15s 盘中监控的现实）→ 64 笔 -62.73U；market_metrics 简化模拟（片内止损）→ -63.79U，与保守口径几乎一致——**回测验证的 +36% 建立在乐观出场模型上，客户端真实行为更接近保守模型**；⑤ 修复后的 08-13 全天 D 纯模拟（成本+过滤器）：信号 174、机会 110、模拟盈亏 -63.79U；⑥ 测试更新（classify_test.go 按成本口径重算期望值），工具测试全过 | 用户要求：①漏斗模拟补手续费/滑点与缺失过滤器，让日报数字=回测口径；②用回测引擎本身重放当天，保证"今天跑的就是昨天验证的那台机器"。重放过程中定位到出场口径（close vs ohlc）是回测与客户端行为分叉的最大单一来源 |
| 2026-08-14 | **全周期复验定案：A/D 策略的"正收益"完全来自乐观出场模型（-exit-mode=close），换成客户端真实的片内出场（ohlc）即破产**（backtest 引擎，2024-01~2026-08 全 842 币，部署参数+手续费0.05%+滑点0.1%）：| A（均仓）| D（爆拉1.5/0.7）| 说明 |；| close（验证口径，只看收盘价）| +36,371U / PF1.61 / 55,277笔 | +48,706U / PF1.65 / 55,277笔 | D−A=+12,335U（+34%，复现文档+36%）；两版笔数相同证明因子只改仓位不改入场 |；| ohlc（客户端现实，片内高低价）| **−991U（2024-02 破产，PF0.55）** | **−991U（2024-02 破产，PF0.52）** | 1,339/1,049 笔即亏完本金，引擎破产保护自动停手 |。结论：① 客户端每 15s 监控盘中价 + 交易所止损单，等价于 ohlc——A/D 在真实出场下全周期亏到破产；② "5m 爆拉仓位因子 +36%" 只是乐观模型下的相对增益，绝对收益本身不存在；③ 与 2026-08-10 全范式回测"5m 粒度无正期望、全部爆仓"的诚实结论一致（那次是 ohlc 口径），08-13 的 D 验证是切回 close 口径后的"虚胖"；④ 后续所有策略必须过 ohlc 口径才能上模拟/实盘，close 口径禁止作为通过标准；⑤ 顺手修正 backtest 中 "close=近似 tick 采样" 的误导注释（tick 采样能看到片内价格，close 是乐观模型，ohlc 才是保守/真实模型） | 用户追问"回测验证过+36%为什么模拟盘会亏"并要求不许甩锅数据：全周期复验证明根因是出场模型——验证用 close（看不到盘中插针），客户端用盘中价（等价 ohlc），同一策略在 ohlc 下 2024-02 即破产；模拟盘亏钱不是偶然，是真实口径的必然 |
| 2026-08-14 | **路1"新考试"通过：收盘判定出场 + 8% 灾难硬止损，全周期转正**（backtest 新增 -hard-sl 灾难硬止损开关，ExitClose 模式下盘中穿透即平仓兜底）：| 考试规则 | A（均仓）| D（爆拉1.5/0.7）|；| 真实盘中规则（现在客户端）| 破产（2024-02）| 破产（2024-02）|；| 路1：收盘判定3%止损/跟踪 + 8%硬止损 | **+28,812U / PF1.45 / 回撤5.9%** | **+37,954U / PF1.47 / 回撤7.5%** |。说明：① 8% 硬止损共兜底 1,402 笔"真崩"单（每笔约 -8%），把 D 的利润从 +48,706U 削到 +37,954U、回撤 5.6%→7.5%——这是买保险的代价；② D−A=+9,142U（+32%），爆拉因子在路1下依然有效；③ 客户端要兑现该数字必须真的改为：撤掉盘中 3% 止损单、每根 5m 收盘后按收盘价判定止损/跟踪、仅保留 -8% 灾难硬止损；④ 风险提示：真崩时 8% 可能不够（跳空穿仓），回测按 min(open,stop) 假设成交，且极端行情多仓同崩可能超模型回撤；⑤ 平均每笔盈利约 0.69U（100U 名义×倍数），非常薄，真实滑点/成交延迟可能吃掉大部分——必须先模拟盘验证再谈实盘 | 用户问"路1是不是在赌 K 线内下砸的力度"：是，但赌的是统计——过去两年插针收盘前大多拉回来。新考试（收盘判定+8%兜底）验证这个赌划得来：全周期 D +37,954U、PF1.47；代价是硬止损砍掉 1,402 次真崩单、回撤升到 7.5% |
| 2026-08-14 | **D 客户端落地路1：收盘判定出场 + 8% 灾难硬止损**：① StrategyConfig 新增 ExitOnClose（默认 false）与 HardStopPct（默认 0），D 版构建 -X 覆盖为 1 / 0.08，前端默认值同步；② order/manager.go PlaceStopOrders：ExitOnClose 时交易所只挂 HardStopPct 灾难硬止损条件单（撤掉盘中 3% 止损与 TRAILING_STOP_MARKET），触发价钳制与校验按硬止损线；③ engine.go：monitorPositions 新增路1分支——硬止损由交易所条件单 + checkStopFallback 兜底；3% 止损 / 2% 激活 / 3% 跟踪改由 checkCloseBarExit 在最新已收盘 5m 推进后按收盘价判定（极值用已收盘 5m 最高价更新，不追盘中插针），新增 exit5mCache / closeExitBar 去重；④ openPositions 初始 CurrentStopPrice 改为硬止损线；⑤ bindings migratePersistedStrategyConfig 缺键补默认（D 构建经 -X 得到 1/0.08），A/B 构建行为不变；⑥ 4 包测试全过 + 前端构建 + D 暂存版 quant-desktop-smart\quant-desktop-new.exe 重建，待用户退出客户端后替换部署；模拟盘验证要点：盘中插针不再触发止损（预期"假插针"单存活率大幅提升）、真崩时 8% 硬止损兜底、跟踪止盈按 5m 收盘判定 | 路1 新考试通过后，用户拍板"改过来"：把客户端出场真正改成回测 close 口径（收盘判定 + 8% 灾难硬止损），让"今天跑的就是昨天验证的那台机器"落地；改完先模拟盘验证数字是否与回测对得上 |
| 2026-08-14 | **防插针系统第一步：插针守卫（WickGuard）落地**：① 新增 internal/strategy/wick_guard.go——多源 + 持续时间确认（用户 Python 伪代码 verify_breakdown 的 Go 实现）：窗口内所有样本的"最新价 + 标记价"都低于止损线且样本数足够，才判定真实击穿；瞬间插针（击穿又弹回）被忽略；窗口按引擎 tick 设置（45s/2 样本），不照搬按秒推送的 3s/3 样本；② wick_guard_test.go 4 个单测：插针不确认 / 数据不足不确认 / 15s tick 场景 / 标记价缺失兜底（单源确认不丢保护）；③ 接入 engine.go checkStopFallback（本地兜底平仓前）：每次击穿 tick 刷新标记价并记录样本，1×兜底时长后要求"标记价也击穿（0.5% 容差）+ 守卫确认"才本地平仓，2×时长强制平仓（防标记价长期滞后导致保护悬空）；④ 3 个既有兜底测试更新为新契约（1×delay 被守卫拦下=疑似插针不平仓，2×delay 强制兜底）；⑤ 明确不照搬 Python 框架的三处：每秒挂 -3% 限价买单（抄底策略+限频风险）、盘中移动止损（路1 破产根因）、保命卖单用限价（真崩可能不成交，8% 灾难单必须市价）；⑥ 4 包测试全过 + D 暂存版重建，待部署 | 用户提供防插针思路与 Python 伪代码（多源验证：最新价=摊位喊价、标记价=官方公告牌；持续 3 秒确认才动作），要求翻译成 Go 并落地；专家适配：核心验证逻辑照搬并升级，三处与路1 冲突的伪代码不照搬，保命单保持市价 |
| 2026-08-14 | **防插针第2道检查（量能验证）回测定案：不采用**（backtest 新增 -wick-surge 信号根低量过滤开关，Road-1 D 全周期对比）：| 变体 | 交易数 | 累计盈亏 | PF | 回撤 |；| 基线（无过滤）| 55,265 | +37,954U | 1.47 | 7.46% |；| 低量<0.5×均值过滤 | 54,909 | +37,505U（−449U）| 1.46 | 7.46% |；| 低量<1.0×均值过滤 | 51,263 | +34,371U（−3,583U）| 1.45 | 7.48% |。结论：**"低量=插针"假设被数据否定**——被过滤的低量信号根反而更赚钱（0.5~0.8× 缩量单笔 +1.01U、<0.5× 极度缩量 +1.29U，均高于平均 +0.8U），过滤它们只减少利润、PF 下降、回撤不降；量能验证不作为过滤器上线；该开关保留为实验项（默认关闭）。第1道（收盘价 vs 标记价）与第3道（信号真实性）需要历史标记价 K 线（data.binance.vision markPriceKlines），待下载数据后验证 | 用户要求把防插针系统剩下三道检查做完，每道先回测"防多少假、误杀多少真"；第2道量能验证按规矩先回测，结果是否定的——这就是"先验证再上线"的意义：没用的规则在浪费代码和时间之前就被数据拦下了 |
| 2026-08-14 | **防插针第1+3道检查（收盘价 vs 标记价可信度）回测通过：采用 1.0% 阈值**：① download 工具新增 -mark 开关（markPriceKlines 月度 zip → data_mark），下载交易最活跃 600 币 × 2024-01~2026-08 共 1.1 亿根标记价 K 线；② backtest 引擎新增 -mark-data 并行流（bar.markClose，按行号与行情 K 线对齐，时间戳不匹配置 0 不做过滤）与 -wick-mark-dev 信号根收盘价 vs 标记价收盘价偏差过滤；③ Road-1 D 全周期对比：| 变体 | 交易数 | 累计盈亏 | PF | 最大回撤 | 净利/回撤 |；| 基线 | 55,265 | +37,954U | 1.47 | 7.46% | 5,088 |；| 偏差>0.5% 过滤 | 54,869 | +37,334U（−620U）| 1.46 | 6.98% | 5,349（+5%）|；| **偏差>1.0% 过滤** | 55,167 | +37,679U（−275U）| 1.46 | **6.46%** | **5,833（+15%）** |。结论：**标记价可信度验证有效**——1.0% 阈值只误杀 98/55,265 个信号（0.18%），利润仅少 0.7%，但最大回撤降 13.4%（7.46%→6.46%），净利/回撤提升 ~15%；被过滤的信号虽平均盈利（+2.8U/笔），但属于"高收益高波动"尾部，砍掉它们换更平缓的曲线划算；该过滤器建议在客户端落地（开仓/收盘判定时拉取已收盘 5m 的标记价 K 线收盘价，偏差 >1.0% 视为可疑信号不动作） | 用户要求防插针三道检查全部回测；第1道（收盘价 vs 标记价）需要历史标记价数据，已下载 600 币全周期并验证：与量能验证相反，这条规则用 0.7% 的利润换 13% 的回撤下降，风险调整后 +15%，是有效的防插针规则，建议上线 |
| 2026-08-14 | **D 客户端落地标记价可信度过滤（防插针，WickMarkDev=1.0%）**：① StrategyConfig 新增 WickMarkDev（%），D 版构建 -X defaultWickMarkDev=1（1.0%），A/B 默认 0=关闭，前端默认值/迁移缺键补默认同步；② rest.go 新增 GetMarkKline5mClose（fapi markPriceKlines，取最新已收盘 5m 标记价收盘价，DRY_RUN 固定 100）；③ engine.go appendKlineCloseCandidates：5m 收盘信号达标后，若标记价收盘价与行情收盘价偏差 > WickMarkDev 且 K 线时间戳匹配 → 落库 ReasonMarkDev（"标记价偏差过滤（防插针）"，RuleBlockReasons 拦截类）+ 跳过该信号（等下一根确认）；标记价拉取失败/无匹配时不误杀（跳过校验）；④ market_metrics ruleReasonCN 新增 mark_dev，漏斗归因自动计入"拦截（该挡）"；⑤ 测试：GetMarkKline5mClose DRY_RUN 单测 + 6 包全过 + 前端构建 + D 暂存版重建，待部署；模拟盘验证要点：收盘价与标记价偏差 >1% 的信号不再开仓，漏斗将出现"标记价偏差过滤"拦截项 | 标记价可信度回测通过后，用户拍板"落地"：把 1.0% 阈值写进客户端开仓链路，让防插针系统第1/3道检查真正生效 |

### 3.4 性能优化与基准（2026-07-27）

**结论**：CPU 路径（滑动窗口、筛选）均为微秒级，无瓶颈；真正瓶颈在入仓流程的网络 I/O 串行放大与 REST 轮询，已由 P0/P1/P2 解决。

基准测试（`go test -run=^$ -bench=. -benchmem ./internal/strategy/`）：

| 基准 | 结果 | 说明 |
| --- | --- | --- |
| SlidingWindow_Add | 30.6 ns/op, 1 alloc | 400 币/tick ≈ 12µs，可忽略 |
| SlidingWindow_GainPct | 14.1 ns/op, 0 alloc | 无优化必要 |
| ScreenSliding（400 币） | 12.1 µs/op | 筛选开销极低 |
| OpenPositions 并发（30 仓） | ~6 ms/op | DRY_RUN 无网络，纯 DB+并发开销 |

### 3.5 待办与备注

- **DRY_RUN GUI 全流程实跑**：完整 GUI 应用依赖窗口与币安网络，当前以集成测试覆盖全逻辑路径（开仓→挂止损→同步→平仓闭环）。如需真机验证，运行 `wails3 dev`（需先 `export PATH="$HOME/go/bin:$PATH"`）。
- **环境注意**：`wails3` 二进制位于 `$HOME/go/bin`，默认 shell PATH 未包含，打包前需手动导出。
- **CGO 交叉编译**：go-sqlite3 依赖 CGO，macOS → Windows exe 需 mingw-w64，建议 Windows 原生构建或 CI。

### 3.6 运行规范（2026-08-04 定调，唯一入口）

| 事项 | 规范 |
| --- | --- |
| **启动应用的唯一方式** | `go build . && ./quant-desktop`（编译后的生产二进制，窗口标题固定为「量化交易客户端」） |
| **禁用启动方式** | `go run .`（go run 只杀包装进程、编译出的临时二进制成孤儿挂窗口）；`wails3 dev`（仅限前端开发，用后必须关闭 dev 窗口与 vite 进程，禁止当作运行方式） |
| **单实例保证** | main.go `acquireSingleInstanceLock()`：临时目录锁文件存 PID，重复启动直接退出（提示"已在运行 PID xxx"），崩溃残留的锁自动抢占；无论何种方式启动都只能有一个窗口 |
| **多开危害** | 多实例并发写同一 SQLite 库（数据库锁/数据错乱）+ 多个窗口混淆 |
| **启动前检查** | `pgrep -f '^\./quant-desktop$'` 确认无残留；有残留先 `kill <PID>` |

**历史事故（2026-08-04）**：go run 孤儿进程（36610）+ wails3 dev 实例（38115）+ 生产二进制（38326）三个实例共存 3 个窗口，且均写同一 simulation 库。已全部清理 + 代码层单实例锁根治。

### 3.7 Windows 打包指南（2026-08-05 更新：Zig 直编译替代 Docker）

> 原方案走 Docker + wails-cross 镜像（约 800MB），2026-08-05 实测 Docker Desktop 故障无法启动，
> 且镜像方案本身存在两个坑（见 3.7.6）。现改为 **Zig 本机交叉编译**：无需 Docker、镜像，
> 仅安装一个 Zig（约 200MB），命令与 wails-cross 镜像内部逻辑完全一致（同款 zcc 包装脚本）。

#### 3.7.1 前置依赖（打包机 = macOS）

| 依赖 | 说明 | 检查命令 |
| --- | --- | --- |
| Zig（0.16+） | CGO 交叉编译器（替代 Docker，含 mingw-w64 头文件） | `zig version`，未装执行 `brew install zig` |
| wails3 CLI | 位于 `$HOME/go/bin/wails3`，PATH 需手动导出 | `export PATH="$PATH:$HOME/go/bin"` |
| pnpm | 前端构建（项目强制，npm 会触发 only-allow 拒绝） | `pnpm --version` |
| Go 1.26.5 | 项目 Go 版本 | `go version` |

#### 3.7.2 打包步骤

```bash
cd /Users/office01/01010101/00ba/quant-desktop
export PATH="$PATH:$HOME/go/bin"

# 1. 构建前端产物（embed 进 exe 的 frontend/dist）
wails3 task common:build:frontend

# 2. 生成 Windows 版本资源（info.json → syso，含产品名/版本/公司等元数据）
wails3 task windows:generate:syso ARCH=amd64   # 产出根目录 wails_windows_amd64.syso

# 3. Zig 交叉编译（CGO_ENABLED=1 保留 SQLite；CC 用包装脚本 scripts/zcc-windows-amd64）
CGO_ENABLED=1 GOOS=windows GOARCH=amd64 \
  CC="$PWD/scripts/zcc-windows-amd64" CGO_CFLAGS="-w -fno-sanitize=all" \
  go build -tags production -trimpath -buildvcs=false \
  -ldflags="-w -s -H windowsgui" -o bin/quant-desktop.exe .

# 4. 清理 syso 产物（Go 打包时自动嵌入，交付无需保留）
rm -f wails_windows_amd64.syso
# 输出：bin/quant-desktop.exe（约 16MB，PE32+ GUI x86-64，Windows 10/11 x64）
```

> ⚠️ 不要用 `CGO_ENABLED=0`：会丢失 SQLite（量化客户端核心存储），不可用于正式分发。
> `scripts/zcc-windows-amd64` 是包装脚本，解决三个问题（详见 3.7.6）：
> ① 剥离 Go 传入的 `-target`（避免与 zig 冲突）；② 把 `-mwindows` 翻译为 `-Wl,--subsystem,windows`
> （否则 exe 是控制台程序、运行时弹黑窗）；③ 关闭 zig 默认 UBSan（否则链接报 undefined symbol）。

#### 3.7.3 程序元数据（已配置）

`build/windows/info.json` → 通过 `wails3 generate syso` 注入 exe 资源：

| 字段 | 值 |
| --- | --- |
| ProductName | 量化交易客户端 |
| CompanyName | QuantDesk |
| FileVersion / ProductVersion | 1.0.0 |
| FileDescription | 币安合约量化交易桌面客户端 |
| LegalCopyright | © 2026 QuantDesk |

#### 3.7.4 历史数据文件夹忽略（自动满足 + 三重保障）

| 层面 | 机制 |
| --- | --- |
| 代码层 | 主程序（quant-desktop）**不引用** `backtest/data`、`backtest/data_funding`（已 grep 验证零引用）；历史数据仅被独立回测工具 `backtest/cmd/*` 使用，不参与主程序构建 |
| 构建层 | `go build` 主程序不编译 `backtest/` 包；`//go:embed` 仅嵌入 `frontend/dist`，历史数据（14G+92M）**不会进入 exe** |
| 仓库层 | `.gitignore` 已排除 `backtest/data/`、`backtest/data_funding/`（不入库） |

因此 exe 启动/运行全程**不读取、不写入、不修改**任何历史数据文件夹，且不影响其他功能（行情/交易/存储全部走币安 API + data/ 运行库）。

#### 3.7.5 验证方法

```bash
# 1. 产物存在且为 Windows GUI 程序（subsystem=2，运行时无黑窗）
ls -lh bin/quant-desktop.exe
file bin/quant-desktop.exe        # PE32+ executable (GUI) x86-64

# 2. exe 内不含历史数据路径（确认未误打包）
strings -a bin/quant-desktop.exe | grep -cE "backtest|data_funding"   # 期望 0
#   注：grep "\.csv" 会命中 WebView2 的 MIME 扩展列表（非本项目数据），属正常

# 3. SQLite 已链接（CGO 生效）
strings -a bin/quant-desktop.exe | grep -c "sqlite3"    # 期望 >0（实测 484）

# 4. 元数据已嵌入（UTF-16 编码，Windows 资源管理器可读）
python3 -c "
data = open('bin/quant-desktop.exe','rb').read()
for kw in ['量化交易客户端','QuantDesk','1.0.0','币安合约量化交易桌面客户端']:
    print(kw, 'OK' if kw.encode('utf-16-le') in data else 'MISSING')"

# 5. Windows 真机（Win10/11 x64）：双击运行 → 窗口「量化交易客户端」弹出
#    - 策略页配置显示默认值（最大持仓 10、追加仓位开、纯跟踪）
#    - 日志无「找不到 backtest/data」类错误（本就不应存在）
#    - 启动后自动探测代理链：本地 10808 → 远端 45.251.241.89 → 7897 自动检测
# 6. 元数据：Windows 资源管理器 → 右键 exe → 属性 → 详细信息
#    显示 产品名称=量化交易客户端 / 版本=1.0.0 / 公司=QuantDesk
```

#### 3.7.6 常见问题

| 问题 | 处理 |
| --- | --- |
| 链接报 `undefined symbol: __ubsan_handle_*` | zig 0.16 默认开启 UBSan，Windows 目标无运行时。包装脚本/`CGO_CFLAGS` 已带 `-fno-sanitize=all`；若自写命令请保留 |
| exe 是控制台程序（`file` 显示 console）/运行时弹黑窗 | Go 外部链接传 `-mwindows`，zig 不支持该标志。包装脚本已翻译为 `-Wl,--subsystem,windows`；验证见 3.7.5-1 |
| Windows 打开黑屏/无窗口 | 需 Microsoft Edge WebView2（Win10 一般自带；缺失时应用安装包 bootstrapper 自动补装） |
| 代理探测全失败 | 确认本机代理软件监听端口；日志会打印每步探测结果 |
| 原先的 Docker 方案 | 保留 `scripts/build-windows.sh` 与 `build/docker/Dockerfile.cross` 备查；Docker Desktop 修复后仍可用（需先配可用镜像源，registry.docker-cn.com 已停服） |

---

## 四、关联文档

- 设计方案：`.trae/documents/trading-automation-design.md`（委托止损改造，已实施完成）
- 实施计划：`.trae/documents/wails-v3-quant-client-plan.md`
- Python 重写规格：`.trae/specs/rewrite-trading-bot-python/`（已全部完成，99 测试通过）

---

## 五、Algo Order API 迁移（2026-07-30）

### 5.1 背景

2025-12-09 起，币安将 USDⓈ-M Futures 条件单（STOP_MARKET / TRAILING_STOP_MARKET / TAKE_PROFIT_MARKET / STOP / TAKE_PROFIT）从 `POST /fapi/v1/order` 强制迁移至 `POST /fapi/v1/algoOrder`。原接口提交条件单返回错误码 `-4120`。

**官方依据**：

- 变更日志：<https://developers.binance.com/docs/derivatives/change-log#2025-11-06>
- 新接口文档：<https://developers.binance.com/docs/derivatives/usds-margined-futures/trade/rest-api/New-Algo-Order>
- 官方论坛确认：<https://dev.binance.vision/t/algo-order-endpoint-question/37372>

### 5.2 改动文件

| 文件 | 改动内容 |
| --- | --- |
| `internal/binance/types.go` | `OrderResult` 新增 `AlgoID`；`OrderInfo` 新增 `AlgoID`/`AlgoStatus`/`ActualOrderID`；新增 Algo 状态常量；删除 `ErrAlgoOrdersUnsupported` |
| `internal/binance/rest.go` | `PlaceStopMarket`/`PlaceTrailingStop` 改用 `NewCreateAlgoOrderService()`（`TriggerPrice`/`ActivatePrice`）；新增 `CancelAlgoOrder`/`GetAlgoOrderStatus`/`GetOpenAlgoOrders`；删除 `algoUnsupported` 降级逻辑 |
| `internal/storage/order_repo.go` | `Order` 结构体新增 `AlgoID`；所有 SQL 查询/插入/扫描增加 `algo_id` 列 |
| `internal/storage/db.go` | `orders` 表 schema 新增 `algo_id` 列；新增 `migrateOrdersColumns()` 迁移函数 |
| `internal/order/manager.go` | `PlaceStopOrders` 删除降级预检和 `ErrAlgoOrdersUnsupported` 处理，写入 `AlgoID`；`CancelRelatedOrders` 按 `AlgoID>0` 走 `CancelAlgoOrder`；`syncExchangeOrders` 按 `AlgoID>0` 走 `GetAlgoOrderStatus`（含触发后实际委托查询）；`RecoverOnStartup` 同时查询普通委托和条件单 |

### 5.3 新旧接口对照

| 项目 | 旧接口（已废弃） | 新接口（Algo Order） |
| --- | --- | --- |
| 端点 | `POST /fapi/v1/order` | `POST /fapi/v1/algoOrder` |
| 新增必填字段 | 无 | `algoType = "CONDITIONAL"` |
| 止损触发价 | `stopPrice` | `triggerPrice` |
| 跟踪止损激活价 | `activationPrice` | `activatePrice` |
| 返回值 | `orderId` | `algoId` |
| 取消接口 | `DELETE /fapi/v1/order` | `DELETE /fapi/v1/algoOrder` |
| 查询挂单 | `GET /fapi/v1/openOrders` | `GET /fapi/v1/openAlgoOrders` |

### 5.4 兼容性

- **旧数据兼容**：`AlgoID == 0` 的旧委托仍走 `CancelOrder` / `GetOrderStatus`
- **DB 迁移安全**：`ALTER TABLE ADD COLUMN` 幂等，不破坏已有数据
- **回滚逻辑保留**：挂单失败仍触发回滚平仓（非 -4120 错误）
- **编译验证**：`go build ./internal/...` 和 `go build .` 均通过
