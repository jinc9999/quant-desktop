# 代码审查记录 — 2026-08-04

> 本文件记录 quant-desktop 退出流程代码审查的结论、证据与修复过程，供后续排查追溯。

## 1. 审查概述

| 项目 | 内容 |
| --- | --- |
| 审查时间 | 2026-08-04 |
| 审查范围 | main.go、internal/binance/ws.go、internal/binance/ws_test.go、internal/bindings/service.go、internal/bindings/service_test.go |
| 审查方法 | TRAE-code-review 流程：2 个独立子代理交叉验证（互不知情），逐行证据链 |
| 触发背景 | 代码审查发现应用退出时 `WsManager.Stop()` 对已关闭 channel 二次 close 触发 panic |

## 2. 审查结论摘要

| 编号 | 问题 | 严重度 | 状态 |
| --- | --- | --- | --- |
| #1 | main.go defer 与 app.OnShutdown 双注册 Shutdown，正常退出必然二次调用 Stop → `close of closed channel` panic | critical | 已修复（上一轮） |
| #2 | `log.Fatalf` 内部 os.Exit(1) 不执行 defer，Run 失败路径 Shutdown 被跳过 | major | 已修复（本轮） |
| #3 | `Shutdown()` 无锁，并发/双重调用下 s.db 判空+Close+置 nil 存在数据竞争（-race 必报） | major | 已修复（本轮） |
| #4 | `Shutdown()` 不等待引擎 goroutine，db.Close 可能与运行中 runOnce 的 DB 写入竞态 | minor | 已修复（本轮） |
| #5 | 审查建议 "could use tagged switch" | — | 确认为误报，未改动 |

## 3. 问题证据链与修复明细

### 3.1 #1 退出 panic（critical，上一轮已修复）

**证据链**：main.go 原 L21 `defer quantService.Shutdown()` 与 L56-58 `app.OnShutdown` 双注册。Wails v3 正常退出时序：

1. OnShutdown 回调先执行 → `Shutdown()` → `ws.Stop()` → `close(ws.stopCh)` 成功；
2. `app.Run()` 返回 → defer 再次执行 `Shutdown()` → `ws.Stop()` 无保护再次 `close(ws.stopCh)` → panic；
3. main goroutine 无 recover，每次退出必然崩溃。

**修复**：

- [main.go](file:///Users/office01/01010101/00ba/quant-desktop/main.go#L21) 删除 `app.OnShutdown` 注册，清理统一由 defer 承担；
- [ws.go#L23](file:///Users/office01/01010101/00ba/quant-desktop/internal/binance/ws.go#L23) `WsManager` 新增 `stopOnce sync.Once`，[Stop()#L164-L169](file:///Users/office01/01010101/00ba/quant-desktop/internal/binance/ws.go#L164-L169) 幂等化；
- 新增回归测试：`TestShutdown_DoubleCall`（bindings）、`TestWsManagerStopIdempotent`/`TestWsManagerStopConcurrent`（binance，后者配合 -race 验证并发安全）。

**验证**：`go test -race` 相关用例全 PASS；`TestShutdown_DoubleCall` 在修复前必 panic，修复后通过。

### 3.2 #2 log.Fatalf 绕过 defer（major，本轮修复）

**证据链**（2/2 代理确认）：[main.go#L58-L60](file:///Users/office01/01010101/00ba/quant-desktop/main.go#L58-L60) `app.Run()` 返回错误时 `log.Fatalf` 等价于 os.Exit(1)，**不执行任何 defer**。因此 Wails 启动失败时 `Shutdown()` 完全跳过：SQLite 未正常 Close（WAL 未 checkpoint）、WS 未优雅关闭、引擎未停止——与 L56-57 注释"资源清理统一由 defer 执行"直接矛盾。Init 失败路径（L18-20）无此问题：defer 尚未注册且 Init 唯一错误分支 db 尚未打开，无资源可泄漏。

**修复**：[main.go#L58-L61](file:///Users/office01/01010101/00ba/quant-desktop/main.go#L58-L61) 失败路径先显式 `quantService.Shutdown()` 再 `log.Fatalf`（Shutdown 已幂等，安全），注释同步改为区分正常/失败两条路径。

### 3.3 #3 Shutdown 无锁数据竞争（major，本轮修复）

**证据链**（2/2 代理确认）：[service.go Shutdown](file:///Users/office01/01010101/00ba/quant-desktop/internal/bindings/service.go#L543-L565) 原实现对 `s.db` 的"判空 + Close + 置 nil"全程无锁：

- 两个 goroutine 并发调用 Shutdown 时，s.db 读-写构成数据竞争（go -race 必报）；
- 与业务方法（如 GetDashboardData 持 RLock 读 s.db）并发时，可能 nil 解引用 panic 或对已关闭连接操作。

**修复**：

- `Shutdown()` 全程持有 `s.mu.Lock()`，与所有持锁业务方法串行化；
- 8 个 db 访问方法补锁 + nil 防护（防止 Shutdown 完成后业务方法读到 nil db）：
  - RLock：GetSavedCredentials、GetDashboardData（db 段）、GetClosedPositions、GetPositions、GetLogs、GetOrders、GetOrderEvents；
  - Lock：CancelOrder（含写操作）；
  - nil 时返回"服务已关闭"错误或空数据，不再 panic。

**死锁分析**（已核验）：引擎路径（runOnce → orderMgr/ws/db/emitBackendError）全程不获取 s.mu；`engine.Stop()` 持有独立的 e.mu，回调 `emitBackendError` 无锁——Shutdown 持 s.mu 期间无死锁环。

### 3.4 #4 引擎 goroutine 未汇合（minor，本轮修复）

**证据链**（2/2 代理确认）：[service.go#L291](file:///Users/office01/01010101/00ba/quant-desktop/internal/bindings/service.go#L291) `go s.engine.Start(s.ctx)` 无汇合；[engine.go#L282-L297](file:///Users/office01/01010101/00ba/quant-desktop/internal/strategy/engine.go#L282-L297) `Stop()` 仅置 running=false + close(stopCh)，不等待 goroutine。若 runOnce 恰在 db 写，Shutdown 的 `db.Close()` 可能先于写入完成，产生 `sql: database is closed`（mattn/sqlite3 返回错误不 panic，仅丢日志）。

**修复**：

- QuantService 新增 `engineWG sync.WaitGroup`（[service.go#L33](file:///Users/office01/01010101/00ba/quant-desktop/internal/bindings/service.go#L33)）；
- StartStrategy 以 `Add(1)` + 包装 goroutine `defer Done()` 启动（[service.go#L291-L296](file:///Users/office01/01010101/00ba/quant-desktop/internal/bindings/service.go#L291-L296)）；
- Shutdown 顺序：cancel → engine.Stop → `engineWG.Wait()` → ws.Stop → db.Close（[service.go#L546-L565](file:///Users/office01/01010101/00ba/quant-desktop/internal/bindings/service.go#L546-L565)）。引擎循环在 stopCh/ctx.Done 后退出，runOnce 有界执行，Wait 不会死锁。

### 3.5 #5 "could use tagged switch" 误报确认

**结论**：main.go 上不成立，2/2 独立子代理一致判定 `tagged_switch_applies = false`、`specific_location = null`。

**证据**：

- main.go 全文仅 2 处 `if err != nil` 错误判空（L18/L58），无类型断言 `x.(T)`、无 if/else 链、无 switch、无 interface{} 分派、无 errors.As/Is；
- Go 类型 switch（tagged switch）适用前提是"同一变量的连续类型断言链"，全仓库唯一符合形态的代码是第三方依赖 `node_modules/flatted.go`（`if s, ok := space.(string); ok {...} else if i, ok := space.(int); ok {...}`），不在 main.go 亦非项目自有代码；
- 该建议大概率来源于审查工具扫描依赖代码或幻觉。**未做任何无效改动**。

**出处追认（2026-08-04 补记）**：经 IDE 诊断核对，QF1003 "could use tagged switch on p.PositionSide" 实际指向 `quant-desktop/tmp_close_positions/main.go#L48`——一个**已删除的临时脚本**（磁盘上无此目录，诊断为残留缓存）。该文件存在时确实满足 tagged switch 适用条件（对 `p.PositionSide` 的类型断言链），与 quant-desktop/main.go 无关。

**残留缓存清除（2026-08-04）**：扩展按 URI 缓存诊断，文件删除后旧诊断仍残留。清除方法：以占位内容重建同 URI 文件 → 触发重新分析（残留 QF1003/shadow 被替换为空）→ 立即删除文件并移除空目录 → 全量诊断归零（`GetDiagnostics` 返回 `[]`）。

## 4. 附带记录：ws_test.go gopls 误报排查

- 现象：`expected ';', found 'EOF'`（1:1）持续出现在 [ws_test.go](file:///Users/office01/01010101/00ba/quant-desktop/internal/binance/ws_test.go)，但 `go vet`/`go test` 均正常，磁盘内容合法；
- 根因：gopls 对新文件 overlay 缓存卡死（编辑器空缓冲区覆盖有效磁盘内容，与 2026-08-04 已记录的嵌套模块 overlay bug 同类）；
- 处理：同内容重写无效（哈希未变不触发 overlay 更新）→ **删除 + 重建文件** 强制重置 gopls 状态，诊断清零；
- 顺带按 Go 1.25 惯用法改写并发测试（`for range 10` + `wg.Go`），GetDiagnostics 归零。

## 5. 验证记录

```bash
cd /Users/office01/01010101/00ba/quant-desktop

# 编译 + 静态检查
go build .
go vet ./internal/...

# 全量测试（含竞态检测）
go test -race ./internal/bindings/ ./internal/binance/
# 结果：ok（全部通过；仅预存在的 macOS 版本 ld 警告，与代码无关）
```

## 6. 已知残余风险（后续排查参考）

| 风险 | 说明 | 建议 |
| --- | --- | --- |
| GetOrderSyncStatus 未加锁 | 仅读 `s.orderMgr`（Shutdown 不置 nil），与退出无竞态，故未改 | 若未来 Shutdown 置 nil orderMgr 需补锁 |
| CancelOrder 锁内网络调用 | 持 `s.mu.Lock()` 期间调用交易所取消 API（有超时），极端下阻塞其他前端调用 | 若需优化可改为锁内取快照、锁外网络、再锁内回写 |
| 引擎 goroutine 无启动失败回报 | wg 仅汇合，不传递 Start 内部 panic | 可后续用 error channel 替代 |

## 7. 本轮文件变更清单

| 文件 | 变更 |
| --- | --- |
| [main.go](file:///Users/office01/01010101/00ba/quant-desktop/main.go) | 失败路径显式 Shutdown；L19/L59 日志格式统一为 `log.Fatalf("中文: %v")`；注释更新 |
| [service.go](file:///Users/office01/01010101/00ba/quant-desktop/internal/bindings/service.go) | Shutdown 加锁 + engineWG 汇合；8 个 db 方法补锁/nil 防护 |
| [说明文档.md](file:///Users/office01/01010101/00ba/quant-desktop/说明文档.md#L180) | 变更记录追加本轮修复 |
