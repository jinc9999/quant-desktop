# 测试报告：最小成交额 1000 万 USDT 限制验证（2026-08-07）

## 1. 验证概述

针对 2026-08-07 变更「最小成交额（MinQuoteVolume）10 万 → 1000 万 USDT」执行系统验证与测试，覆盖：

1. 完整交易系统构建与启动（前端 + 后端 + 依赖组件）
2. 交易验证关键路径日志（订单创建 / 金额校验 / 交易提交）
3. 前端配置界面显示值与输入框限制
4. 后端边界/功能测试 + 前端一致性 + 端到端协同验证
5. 测试结果记录

**结论：全部通过。** 1000 万成交额限制在粗筛（buildKlineOpenMap）→ 流动性筛选（ScreenSliding）→ 开仓前校验（openPositions）三层均真实生效，日志完整打印判断过程（原始金额→校验规则→限制匹配→决策结果）。

## 2. 验证环境

| 项目 | 值 |
| --- | --- |
| 操作系统 | Windows（Asia/Shanghai） |
| Go | 1.25.12（CGO_ENABLED=1，w64devkit gcc） |
| Node / pnpm | v22.14.0 / 11.20.0 |
| 框架 | Wails v3（桌面 GUI）+ Vue3 + TypeScript + SQLite |
| 币安客户端 | DRY_RUN 模式（模拟交易，不触碰真实账户） |
| 本地代理 | 127.0.0.1:10808（http，测试中自动检测可用） |

## 3. 关键路径日志实现（对应任务 2a/2b）

在 [engine.go](file:///d:/0001_ba-A - 02/quant-desktop/internal/strategy/engine.go) 成交额校验关键路径新增两组日志：

### 3.1 每 Tick 全市场成交额校验日志（`logQuoteVolumeFilter`，runOnce 3.6 节）

- **汇总行**：每次 Tick 输出达标数 / 被过滤数 / 阈值；
- **逐条判断过程**：仅对成交额接近阈值（≥ 阈值的 50%）却被过滤的合约打印（最多 10 条），远离阈值的低流动性币只计入汇总（防刷屏）；
- 拦截动作仍由筛选层执行，本函数只负责可观测性，不改业务逻辑。

### 3.2 开仓前成交额最终校验日志（`openPositions`）

每个候选必经判断：满足 → `决策=允许开仓`，不满足 → `决策=拒绝开仓`（`continue` 跳过）。

**真实运行日志样例**（`TestOpenPositions_MinQuoteVolumeCheck` 实测输出）：

```
[Strategy] 成交额校验(开仓前): LOWVOLUSDT 原始金额=9000000USDT 校验规则=24h成交额>=10000000USDT 限制匹配=不满足 决策=拒绝开仓
[Strategy] 成交额校验(开仓前): OKVOLUSDT 原始金额=10000000USDT 校验规则=24h成交额>=10000000USDT 限制匹配=满足 决策=允许开仓
[ORDER] OKVOLUSDT 止损保护委托挂出成功 持仓ID=1 止损价=90.000000 跟踪激活价=105.000000
[Strategy] 开 OKVOLUSDT: 价格=100.000000 数量=0.500000 止损=90.000000
```

日志覆盖完整判断链：原始金额 → 校验规则 → 限制匹配 → 最终决策 → 订单创建 → 止损委托挂出（交易提交）。

## 4. 后端测试结果（对应任务 4a）

执行 `go test ./internal/... -count=1`，**6 个包全部通过**：

```
ok  quant-desktop/internal/binance   29.256s
ok  quant-desktop/internal/bindings  26.295s
ok  quant-desktop/internal/order      6.660s
ok  quant-desktop/internal/risk       0.650s
ok  quant-desktop/internal/storage    1.539s
ok  quant-desktop/internal/strategy  49.002s
```

### 4.1 边界值场景（等于 / 接近 / 超过 1000 万）

| 场景 | 成交额 | 预期 | 测试 | 结果 |
| --- | --- | --- | --- | --- |
| 等于阈值（>= 语义） | 10,000,000 | 入选 | `TestScreenSliding_Boundary_MinVolumeNewDefault` | ✅ |
| 接近但不足 | 9,990,000（999 万） | 过滤 | `TestScreenSliding_Boundary_MinVolumeNewDefault` | ✅ |
| 略超阈值 | 10,500,000 | 入选 | `TestScreenSliding_Boundary_MinVolumeAbove` | ✅ |
| 显著超过 | 150,000,000（1.5 亿） | 入选 | `TestScreenSliding_Boundary_MinVolumeAbove` | ✅ |
| 接近但不足 | 9,900,000（990 万） | 过滤 | `TestScreenSliding_Boundary_MinVolumeAbove` | ✅ |

### 4.2 粗筛路径（buildKlineOpenMap，K 线信号模式）

| 场景 | 成交额 | 预期 | 测试 | 结果 |
| --- | --- | --- | --- | --- |
| 恰好 1000 万 | 10,000,000 | 拉取 K 线 | `TestBuildKlineOpenMap_NewMinQuoteVolume` | ✅ |
| 低于阈值 | 9,000,000 | 跳过 | `TestBuildKlineOpenMap_NewMinQuoteVolume` | ✅ |

### 4.3 开仓前最终校验（openPositions）

| 场景 | 成交额 | 预期 | 测试 | 结果 |
| --- | --- | --- | --- | --- |
| 不达标 | 9,000,000 | 拒绝开仓（不产生持仓） | `TestOpenPositions_MinQuoteVolumeCheck` | ✅ |
| 恰好阈值 | 10,000,000 | 允许开仓（DRY_RUN 真实开仓+挂 2 条止损） | `TestOpenPositions_MinQuoteVolumeCheck` | ✅ |

### 4.4 日志可观测性（logQuoteVolumeFilter）

| 断言 | 测试 | 结果 |
| --- | --- | --- |
| 汇总行含 达标数/被过滤数/阈值 | `TestLogQuoteVolumeFilter` | ✅ |
| 接近阈值被过滤合约逐条打印 原始金额/校验规则/限制匹配/决策 | `TestLogQuoteVolumeFilter` | ✅ |
| 远离阈值合约只计入汇总不刷屏 | `TestLogQuoteVolumeFilter` | ✅ |

### 4.5 持久化迁移（兼容性）

| 场景 | 测试 | 结果 |
| --- | --- | --- |
| 旧持久化配置 100000 启动自动迁移为 10000000 | `TestMigratePersistedConfig_OldMinQuoteVolume` | ✅ |
| 新值 10000000 不重复迁移 / 用户自定义值不覆盖 | `TestMigratePersistedConfig_NoChange` | ✅ |
| 非法 JSON 返回错误回退默认值 | `TestMigratePersistedConfig_BadJSON` | ✅ |
| 默认配置 MinQuoteVolume = 10000000 | `rest_test.go` 断言 | ✅ |

## 5. 实际交易模拟验证（对应任务 2 / 4c）

本应用为 Wails 桌面 GUI，沙箱环境无法弹出窗口（WebView2 需写 AppData），以 **DRY_RUN 集成测试模拟完整交易闭环**验证：

- `TestOpenPositions_DryRun`：候选 → 开仓（DB 新增 OPEN 持仓 + 2 条活跃止损委托）→ 完整闭环 ✅
- `TestOpenPositions_MinQuoteVolumeCheck`：不达标候选被拒、达标候选开仓（见 §3.2 日志）✅
- `TestRunOnce_DryRun`：单个 Tick 全流程（行情→喂价→筛选→开仓→监控）无 panic ✅

## 6. 前端检查结果（对应任务 3 / 4b）

### 6.1 显示值

- [index.vue L26](file:///d:/0001_ba-A - 02/quant-desktop/frontend/src/views/strategy/index.vue#L26)：`minQuoteVolume: 10000000` 默认值 ✅
- `loadConfig` 直接透传后端 `cfg.minQuoteVolume`，与后端一致 ✅
- TS 绑定模型 `StrategyConfig.minQuoteVolume` 存在（[models.ts L16](file:///d:/0001_ba-A - 02/quant-desktop/frontend/bindings/quant-desktop/internal/binance/models.ts#L16)）✅

### 6.2 输入框限制与提示

- 输入框 `el-input-number :min="10000" :step="10000"`：可输入/步进到达 1000 万整（10000000 为 10000 整数倍），边界值可达 ✅
- **本次新增 tooltip**（[index.vue L511](file:///d:/0001_ba-A - 02/quant-desktop/frontend/src/views/strategy/index.vue#L511)）：明确「24h 累计成交额下限（USDT），默认 1000 万（10,000,000）；低于该值的合约不进入候选」，与其他字段提示风格一致 ✅
- 输入 <10000 由 el-input-number 自动纠正至下限（element-plus 标准边界处理）✅
- 前端 `vue-tsc --noEmit --skipLibCheck` typecheck 通过 ✅
- `pnpm build` 构建成功（2.3 MB）✅

### 6.3 说明

`min=10000`（10 万）为**合法输入下限**而非阈值本身——用户显式调低阈值（如 50 万）属于合法配置（仅旧默认值 100000 会被迁移覆盖）。1000 万为默认值与推荐值，已在 tooltip 明示。

## 7. 端到端验证（对应任务 1 / 4c）

| 步骤 | 结果 |
| --- | --- |
| 检查运行实例 | 发现 D:\9999 旧实例（8/5 构建，含 10 万旧配置），已停止并释放单实例锁 |
| 检查应用 DB 持久化配置 | data 下 5 个模式库均无已保存 `strategy:cfg` → 系统以默认值 1000 万运行（无需迁移） |
| 后端构建 | `go build -o bin\quant-desktop.exe .` 成功（32.41 MB，嵌入最新前端 dist） |
| 前端构建 | `pnpm build` 成功（2.3 MB），typecheck 通过 |
| 全量测试 | 6 包 `go test ./internal/...` 全过 |
| 实际启动 | GUI 启动被沙箱拦截（WebView2 需写 AppData\Roaming，超出工作区权限）；**exe 已就绪，用户可双击 `bin\quant-desktop.exe` 启动** |
| 前后端协同 | DRY_RUN 集成测试验证 金额校验→开仓决策→订单创建→止损委托 全链路，限制条件前后端一致 |

## 8. 遗留事项

1. **GUI 手动启动**：沙箱无法弹出窗口，需用户手动双击 `bin\quant-desktop.exe`（启动后策略默认 1000 万；「应用到项目」可持久化）。
2. **实盘/测试网真实下单**：DRY_RUN 已验证逻辑闭环；真实账户下单需在 GUI 中配置 API Key 后进行。
3. **输入框 min 语义**：若需强制阈值不可低于 1000 万（而非仅默认值），可将 `:min` 改为 10000000，但会牺牲阈值下调的灵活性，建议保持现状。
