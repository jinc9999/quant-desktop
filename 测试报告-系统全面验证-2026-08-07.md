# 测试报告：系统全面验证与检测（2026-08-07）

## 1. 检测范围

| 任务 | 内容 | 结果 |
| --- | --- | --- |
| 1 | 启动 quant-desktop.exe，确认加载并进入主界面 | ✅ 通过 |
| 2 | 1000 万成交额限制功能验证（含自定义高于 1000 万的阈值） | ✅ 通过 |
| 3 | 新币过滤功能前端显示与生效验证 | ✅ 通过 |
| 4 | 挂单失败全面检测（错误提示 / 网络 / 多交易对 / 日志定位） | ✅ 通过 |
| 5 | 全面功能检测（策略 / 回测 / 实盘 / 数据同步 / 风控） | ✅ 通过 |

**总体结论：所有核心功能正常，未发现功能性缺陷。** 详见各节。

---

## 2. 任务 1：应用启动与主界面

- 启动方式：`Start-Process bin\quant-desktop.exe`（配合 `WEBVIEW2_USER_DATA_FOLDER` 指向工作区，规避沙箱对 AppData 的写入限制）
- 验证结果：
  - 进程存活且稳定（PID 38068，运行 4.7+ 分钟，内存 86.6MB）
  - 主窗口存在，标题「量化交易客户端」（MainWindowHandle 12650304）→ **已进入主操作界面**
  - WebView2 渲染进程随启动正常拉起（17:22:50 生成 6 个子进程）
  - 单实例锁正常占用（`%TEMP%\quant-desktop.lock`），唯一实例
- 备注：界面交互（点击按钮等）需用户本人操作，本报告以代码路径 + 后端状态佐证功能正确性。

## 3. 任务 2：1000 万成交额限制验证

### 3.1 限制机制说明

`MinQuoteVolume` 为**24h 累计成交额过滤阈值**（默认 1000 万 USDT），命中"低于阈值"的合约**不进入候选**（设计上为静默过滤，不弹错误），决策过程写入日志：`原始金额→校验规则→限制匹配→决策`。

### 3.2 验证结果

| 场景 | 设置 | 成交额 | 预期 | 测试 | 结果 |
| --- | --- | --- | --- | --- | --- |
| 等于阈值 | 1000 万 | 10,000,000 | 入选 | `TestScreenSliding_Boundary_MinVolumeNewDefault` | ✅ |
| 接近但不足 | 1000 万 | 9,990,000 | 过滤 | 同上 | ✅ |
| 略超/显著超过 | 1000 万 | 10,500,000 / 150,000,000 | 入选 | `TestScreenSliding_Boundary_MinVolumeAbove` | ✅ |
| **用户调高阈值** | **2000 万** | 25,000,000 / 15,000,000 | 2500 万入选，1500 万被过滤 | `TestScreenSliding_CustomHigherThreshold`（本次新增） | ✅ |
| 开仓前最终校验 | 1000 万 | 9,000,000 / 10,000,000 | 拒绝 / 允许开仓 | `TestOpenPositions_MinQuoteVolumeCheck` | ✅ |

- 前端输入框无最大值限制，可自由设置高于 1000 万的阈值（如 2000 万），`toBackendConfig` 原样透传；后端按 `>=` 语义比较，阈值上调即过滤更严格。
- **说明**：设置阈值后执行交易不会出现"错误弹窗"——不达标合约被静默剔除（含日志 `决策=拒绝开仓`），这是筛选器的设计行为，非缺陷。若需"提示"，可在前端候选/日志页查看被过滤记录。

## 4. 任务 3：新币过滤前端显示与生效

### 4.1 前端显示（[strategy/index.vue](file:///d:/0001_ba-A - 02/quant-desktop/frontend/src/views/strategy/index.vue)）

- **「新币过滤」开关**（`enableNewListingFilter`）：默认开启（`true`）
- **「过滤天数(天)」输入框**（`newListingMinDays`）：默认 60，范围 0~365，tooltip 说明"上市天数小于等于该值的合约被过滤，0=关闭"
- 两者均映射到后端 `StrategyConfig` 字段，可持久化（「应用到项目」）

### 4.2 生效验证

| 场景 | 测试 | 结果 |
| --- | --- | --- |
| 上市 10 天 ≤ 60 天 → 拦截（含拦截日志） | `TestBuildNewListingBlocked_Logs` | ✅ |
| 拦截日志实测：`[Strategy] 新币过滤: 拦截 NEWUSDT（上市日期 2026-07-28，上市天数 10 天 <= 阈值 60 天）` | — | ✅ |
| 上市天数计算（自然日向下取整/未知-1） | `TestListingDays` | ✅ |
| 开关关闭 → 不拦截 | `TestOpenPositions_NewListingFilterDisabled` | ✅ |
| 未知上市日期 → 放行（不误伤） | `TestOpenPositions_NewListing_UnknownDateAllowed` | ✅ |

## 5. 任务 4：挂单失败全面检测

### 5a. 挂单失败错误提示（[order/manager.go](file:///d:/0001_ba-A - 02/quant-desktop/internal/order/manager.go)）

- 开仓挂单 3 步（止损市价单 → 跟踪止损 → 固定止盈），任一步失败：
  - **指数退避重试** `retryWithBackoff`（默认重试次数，退避 `delay×2^attempt` + ±20% 抖动），每次失败打印 `[ORDER] 操作失败，第 N/N 次重试...`
  - 重试仍失败 → **回滚**：`[ORDER] ❌ symbol 止损条件单挂出失败，触发回滚 stopPrice=...: <币安错误>`，自动平仓 + 取消已挂委托 + 返回结构化错误 `挂止损条件单失败（已重试 N 次）`
- 结构性错误码拉黑（[engine.go](file:///d:/0001_ba-A - 02/quant-desktop/internal/strategy/engine.go#L42-L53)）：`-2027 仓位超限 / -4005 数量超限 / -4028 杠杆无效 / -4164 名义价值不足 / -2019 保证金不足 / -4131 市价单被拒 / -4411 未签协议` → 拉黑 12h 防刷屏；普通失败 5 分钟冷却

### 5b. 网络连接与 API 可用性

- 连接诊断 `TestConnection`（3 个测试）全部通过；运行时自动探测到可用本地代理 `127.0.0.1:10808（http）`
- 代理候选链 + TCP/HTTP 双协议探测 + SOCKS5 支持，均有对应实现与测试

### 5c. 多交易对 / 价格 / 数量挂单（DRY_RUN 全链路）

| 测试 | 覆盖 |
| --- | --- |
| `TestPlaceStopOrders_DryRun` | 多币种开仓 + 止损/跟踪止损挂单成功 |
| `TestPlaceStopOrders_Idempotent` | 重复挂单幂等保护 |
| `TestCancelRelatedOrders` | 失败回滚时取消关联委托 |
| `TestSyncOrders_DryRun` / `TestFilledCloseLoop_DryRun` | 委托同步与条件单触发平仓闭环 |
| `TestRetryWithBackoff` | 重试退避与最终失败返回 |
| 引擎 DRY_RUN 测试 | BTCUSDT(50000U/0.001张)、OKVOLUSDT(100U/0.5张)、NEWUSDT(10U/5张) 等不同价位/数量组合开仓成功 |

### 5d. 日志定位

- 运行期生命周期日志落库 **`trade_logs` 表**（模块归属 `strategy/order/binding`），实测含真实交易记录：`开LONG成功`、`止损保护委托已挂出: 止损价=... 跟踪激活价=...`、`条件单触发平仓 ... reason=TRAILING_STOP filledPrice=...`
- 控制台日志 `[ORDER]/[Strategy]` 前缀用于运行期实时排障；挂单失败含币安错误码与回滚路径，可定位到具体模块与原因

## 6. 任务 5：全面功能检测

| 模块 | 检测方式 | 结果 |
| --- | --- | --- |
| 策略创建与编辑 | `TestPersistConfig_Roundtrip`（写库回读一致）+ 前端表单映射 | ✅ |
| 回测功能 | 实际运行 2 个月回测：567 币 / 17568 时间片 / 56.4s，输出完整绩效报告（374 笔、胜率 57.49%、PF 0.52、最大回撤 98.81%）、report.svg、trades.csv | ✅（回测引擎 24h 成交额阈值独立为 5 万，不影响交易系统 1000 万） |
| 实盘交易 | DRY_RUN 引擎闭环测试（开仓 → 挂止损 → 同步 → 移动止盈平仓） | ✅ |
| 数据同步 | WS 行情管理器测试 + REST 回退机制 + WS 缓存（binance 包全过） | ✅ |
| 风险控制 | 熔断器测试（日亏/回撤触发停止开仓）risk 包全过 | ✅ |
| 全量回归 | `go test ./internal/...` 6 包全过 | ✅ |

## 7. 发现的问题与复现步骤

| # | 问题 | 影响 | 状态 |
| --- | --- | --- | --- |
| 1 | **旧版实例占用单实例锁**：D:\9999\quant-desktop.exe（8/5 构建）仍在运行，阻止新构建启动 | 需先关闭旧进程 | 已解决（本次已停止旧实例并释放锁） |
| 2 | **GUI 启动在沙箱环境受限**：WebView2 需写 `AppData\Roaming\quant-desktop.exe\EBWebView`，工具沙箱默认拦截 | 仅限自动化环境；用户本机双击 exe 无此限制 | 已规避（`WEBVIEW2_USER_DATA_FOLDER` 指向工作区） |
| 3 | 本次回测（2026-06~07）为亏损区间（-988U） | 属行情结果非功能缺陷 | 不适用，功能正常 |
| 4 | 应用 DB 中无已保存策略配置 → 系统以默认值运行（1000 万 / 新币过滤开） | 无 | 预期行为 |

## 8. 结论

- **应用可正常启动并进入主界面**（当前实例运行中，窗口标题「量化交易客户端」）。
- **1000 万成交额限制** 在 粗筛 / 流动性筛选 / 开仓前校验 三层真实生效，自定义更高阈值（2000 万）验证通过；不达标合约静默过滤并记录判断日志。
- **新币过滤** 前端开关与天数参数存在且可配置，后端拦截逻辑（含日志）验证通过。
- **挂单失败** 具备完整错误提示（币安错误码 + 重试 + 回滚）、网络诊断、多交易对覆盖与日志定位能力。
- **策略 / 回测 / 实盘（模拟）/ 数据同步 / 风控** 全部正常。

> 注：本次新增测试 `TestScreenSliding_CustomHigherThreshold`（自定义高阈值场景）。应用当前仍在运行，用户可在界面点击「测试连接」做在线诊断、点击「启动策略」观察实时成交额过滤日志。
